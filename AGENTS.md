# AGENTS.md — 감도 (GAMDO) 프로젝트 컨텍스트

> **이 파일의 목적:** 새 세션(사람·AI 에이전트 무관)이 이 파일 하나만 읽고 프로젝트를 정확히 이해하고 작업에 착수할 수 있게 한다.
> **읽는 규칙:** 이 파일의 "확정 결정"은 다른 모든 문서보다 우선한다. 문서 간 충돌을 발견하면 §6의 우선순위 표를 따르고, 오래된 서술을 이 파일 기준으로 고쳐라.

---

## 1. 프로젝트 한 줄 요약

**감도(GAMDO)** — 사용자가 좋아하는 사진을 고르면, 그 느낌으로 찍는 법을 촬영 화면의 **시각 오버레이**로 보여주고, 찍힌 결과를 같은 감성으로 자동 보정·생성 복구하는 **개인화 카메라 Android 앱**. (코드네임: MOODFRAME)

- 슬로건: "원하는 무드로 찍고, 원하는 무드로 살린다"
- 정체성: 사진을 대신 만들어주는 AI가 아니라, **실제로 찍은 사진을 중심에 두는 카메라 도구**
- 핵심 차별점: 촬영 전(Pre-capture 가이드)과 촬영 후(Post-capture 복구)를 하나의 흐름으로 연결. 기준은 "좋은 사진"이 아니라 **"사용자가 좋아하는 사진과 비슷한가"**

## 2. 팀·기간·목표

- **2인 개발, 7일 프로토타입.** 담당 A(모바일·카메라), 담당 B(백엔드·CV·생성 + 온디바이스 로직 모듈 작성)
- 목표는 상용 베타가 아니라 **데모 시나리오 2개를 실기기에서 끊김 없이 시연**:
  - 시나리오 A: 레퍼런스/스타일 따라 찍기 (분석 → 오버레이 표시 → 사용자가 맞춰 수동 촬영 → 색감 적용 → 비교)
  - 시나리오 B: 망한 사진 살리기 (진단 → 기본·스타일 보정 → 생성형 객체 제거/여백 확장 → 슬라이더 비교)

## 3. 확정 결정 (이것이 최종 — 다른 문서의 오래된 서술보다 우선)

| # | 결정 | 내용 | 비고 |
|---|---|---|---|
| D1 | 플랫폼 | **Android 단일, Kotlin 네이티브 + Jetpack Compose** | 초기 RN 검토는 폐기됨. iOS 없음 |
| D2 | 가이드 방식 | **시각 오버레이 전용**: 추천 프레임(브래킷) + 인물 배치 실루엣 + 수평선만 표시. **텍스트 지시 문구·방향 화살표·일치도 게이지·자동 촬영은 만들지 않는다** (컷이 아니라 제품 결정). 촬영은 사용자가 수동 셔터로. 고정 레이아웃 슬롯은 채움·비움 상태나 색상 변화를 표시하지 않는다. | 구버전 문서에 안내 문구·자동 촬영·슬롯 상태 표시가 남아 있으면 이 결정이 우선 |
| D3 | 구도 제안 | 영뽀또식 **제안형**: 카메라를 들면 장면(리딩 라인·개방 공간·수평선)을 스캔해 최적 구도를 오버레이로 먼저 제안. 물체 종류 라벨은 필수가 아니며, 객체 박스의 개수·위치·배치로 일반 레이아웃을 만든다. 얼굴 박스가 있으면 인물 슬롯과 물체 슬롯을 함께 표시한다. | 1차 구현에서는 객체가 안정적으로 인식될 때까지 탐색 상태를 유지한다. 인식 실패 시 고정 레이아웃을 임의로 표시하지 않는다. 우측 상단 레이아웃 버튼으로 사용자가 고정 레이아웃을 선택하면 즉시 현재 자동 레이아웃을 대체하고 촬영 중에도 변경할 수 있다. 자동 레이아웃으로 돌아가거나 재탐색할 때만 고정을 풀고 다시 탐색한다. 실기기 검증에서 인식률이 낮다고 판정될 경우에만 후속 변경으로 자동 탐색 실패 fallback을 도입한다. |
| D4 | 계정·데이터 | **로그인 없음.** 디바이스 UUID만. **선호도·프로필·피드백·세션 기록은 전부 앱 로컬(Room) 저장** — 서버는 사용자를 저장하지 않는 무상태 처리기 | 앱 삭제 = 데이터 소실(온보딩에 고지) |
| D5 | 서버 | FastAPI + SQLite(작업 추적 3테이블: edit_jobs, edit_job_files, schema_migrations) + 로컬 디스크 + DB 폴링 워커. **API 4개뿐**: `GET /presets`(정적 JSON), `POST /references/analyze`(무저장, 응답 후 즉시 파일 삭제), `POST /edit-jobs`, `GET /edit-jobs/{id}`(1초 폴링) | PostgreSQL·Redis·S3·로그인 API는 확장 시. 온보딩 프로필 생성·피드백 반영은 온디바이스 로직 |
| D6 | 스타일 | 프로토타입 **6종**: Clean Social, Candid Feed, Bright Review, Soft Film, Casual Portrait, Night Street. StylePreset은 composition(구도)과 color(보정) 분리 구조의 JSON, 서버 정적 파일 + 앱 번들 폴백 | 2026-07-24 오너 확정(기존 3종 서술 폐기). `/presets`·번들 `presets.json`·`P1_Plan_1.md`와 일치 |
| D7 | 생성형 | 자체 GPU + ComfyUI headless. **객체 제거(LaMa) 1순위**, 큰 영역·여백 확장은 FLUX.1 Fill [dev](비상업 라이선스 — 상용화 시 교체). `GenerativeEditProvider` 인터페이스로 공급자 교체 가능. Day 1에 Gemini 무료 티어와 품질 비교 후 확정 | 명시적 실행 시에만 서버 업로드 |
| D8 | 안전·개인정보 | 얼굴·신체·나이·인종 변경 기능 **자체를 만들지 않음**. 생성 결과는 얼굴 임베딩(InsightFace) 전후 비교로 검증, 실패 시 기본 보정 폴백(사용자에게 실패 미노출). 원본 비파괴, 서버 업로드분 즉시 삭제, EXIF 위치 스트립, 임베딩 저장 금지 | 스키마 차원에서도 강제됨 |
| D9 | 촬영 범위 | 1인 인물 우선, 비율 4:5·1:1만 | 2인·음식·풍경 특화는 후속 |
| D10 | 브랜드 | 이름 "감도"(ISO 감도 + 감성의 정도). 다크 차콜 UI, 사진이 주인공, 전문 용어 금지("삼분할" 대신 일상 언어), 영뽀또의 정보 구조는 참고하되 시각 표현(색·아이콘·서체)은 독자 설계 | |
| D11 | 화면 구조(UX) | **간결화(t2) 채택** — 무드/홈 화면과 4탭 하단바를 만들지 않는다. **카메라가 곧 홈**(온보딩 완료 시 실행하면 카메라 직행), 앨범은 카메라에서 진입, 편집은 앨범에서 사진을 탭. 온보딩은 **취향 카드 선택 → "내 감도 저장"** 2단계(소개·개인정보 안내는 후속). 단일 강조색은 **세이지**(`#A3BFA0`, 버튼 `#8FAE8B`) — D2의 "민트"를 이 색으로 확정. 차콜 배경 `#151714`(카메라 `#0C0D0B`) | 2026-07-24 오너 결정(`감도 화면 디자인.dc.html`의 **turn 2**). 구버전 문서의 "무드 탭·4탭 하단바·홈 화면·styleExplore·myStyle" 서술보다 우선 |
| D12 | 장면·슬롯 계약 | 물체 종류는 선택 정보이며 인식 통과 조건이 아니다. 유효한 객체 박스가 안정적으로 유지되면 라벨이 없어도 일반 슬롯을 만든다. 슬롯은 최대 4개이며, 5개 이상이면 크고 중앙에 가까운 주요 물체를 우선한다. 사람은 얼굴 박스만 있어도 인물로 인정하고, 얼굴이 있으면 인물 슬롯과 물체 슬롯을 함께 표시한다. | 세그멘테이션은 외곽선 정밀화용이며 필수가 아니다. 물체가 하나로 합쳐진 박스는 하나의 슬롯으로만 처리한다. 최근 5프레임 중 3프레임에서 같은 객체가 검출될 때만 레이아웃을 확정하며, 시간 제한 없이 그 전까지 탐색한다. 확정 후에는 재탐색 전까지 레이아웃을 유지한다. |
| D13 | 레이아웃 제어 | 기본 모드는 자동 탐색 레이아웃이다. 사용자는 우측 상단 레이아웃 버튼에서 고정 레이아웃을 선택할 수 있고, 촬영 중에도 선택을 변경할 수 있다. | 레이아웃 버튼의 시각·인터랙션 상세는 P1과 별도 계약한다. 사용자가 고정 레이아웃을 선택하면 자동 탐색 결과를 즉시 대체한다. 재탐색은 자동 레이아웃의 고정을 풀고 현재 장면을 다시 분석하는 동작이다. 카메라 화면을 벗어나면 세션을 종료하고 선택을 폐기한다. 앱 종료·갤러리 이동·다른 화면 이동 뒤에는 항상 자동 탐색으로 시작하며, 고정 레이아웃 선택을 세션 간 보존하지 않는다. |
| D14 | 촬영 후 처리 우선순위 | 감도의 핵심은 **촬영 전 구도 가이드**다. 사용자가 레이아웃을 참고해 수동 촬영한 뒤 원본을 보존하고, 선택한 감도 스타일의 색감 보정을 후속 적용한다. | 구도 가이드는 사진을 자동 자르거나 재배치하지 않는다. 색감 보정은 로컬 우선이며, 객체 제거·여백 확장 등 생성 AI는 사용자가 `사진 살리기`를 명시적으로 선택할 때만 실행한다. |
| D15 | 구도 품질 기준 | 물체가 계속 화면에 보이는 경우 최초 레이아웃은 2초 이내 표시하고, 기준 기기에서 객체 분석 평균 8FPS 이상을 목표로 한다. 최근 5프레임 중 3프레임 안정 판정은 유지한다. | 정확도는 물체 이름 분류가 아니라 분리된 주요 피사체 수·위치가 안정적으로 슬롯에 반영되는지로 평가한다. 확정 전 순간 누락은 재탐색하고, 확정 후 레이아웃은 유지한다. |
| D16 | 처리 위치 | 실시간 촬영 가이드(얼굴·포즈·물체 감지, 슬롯 수·위치 계산, 레이아웃 선택)는 전부 온디바이스에서 처리한다. | 카메라 프레임을 실시간 서버로 업로드하지 않으며, 서버 분석으로 자동 전환하지 않는다. 레퍼런스 분석과 생성형 편집만 사용자의 명시적 선택 후 서버를 사용한다. |
| D17 | 탐색 상태 표시 | 객체가 없거나 아직 안정적으로 확정되지 않은 동안에는 작고 방해되지 않는 로딩 표시를 계속 노출한다. 레이아웃이 확정되면 로딩 표시를 숨긴다. | 인식 실패·대기 상태에서 행동을 지시하는 문구나 오류 메시지는 표시하지 않는다. 레이아웃이 없어도 수동 촬영은 허용한다. |
| D18 | 서버·생성 실패 | 레퍼런스 분석이나 생성형 편집이 실패해도 앱 전체를 중단하지 않는다. 원본과 기본 로컬 보정 결과를 유지하고, 생성 결과가 없으면 해당 결과 선택지만 숨긴다. | 네트워크가 없어도 촬영·기본 보정·저장은 가능하다. 서버 기능은 사용자가 다시 명시적으로 실행할 수 있다. |

## 4. 아키텍처 요약

```text
[Android 앱 — Kotlin/Compose]                [서버 — FastAPI+SQLite]      [GPU — ComfyUI]
 CameraX(Preview/Analysis/Capture)            edit_jobs 큐·폴링    ──────→  LaMa 객체 제거
 ML Kit(얼굴·포즈) + MediaPipe(분할)          /references/analyze(무저장)   FLUX.1 Fill 확장
 센서(수평·흔들림)                            /presets 정적 서빙            InsightFace 검증
 AlignmentEngine(오버레이 좌표+안정화)
 LocalEditor(수평→노출→스타일 LUT, 2초 내)
 Room(로컬 14테이블 = 데이터 원천)
```

- 온디바이스: 감지, 오버레이, 기본 보정, 프로필 생성·피드백 반영, 추천 (오프라인 전부 동작)
- 서버 필요: 레퍼런스 분석, 생성형 편집만
- B가 작성해 A가 통합하는 순수 Kotlin 모듈 4개(JVM 단위 테스트 필수): `FrameFeatureCalculator`, `AlignmentEngine`, `ProblemDiagnoser`, `ProfileEngine`
- 임계값·가중치는 전부 `guide_config.json` 외부화(리허설 현장 튜닝용)

## 5. 프로젝트 문서 목록 (이 순서로 읽어라)

| 파일 | 내용 | 상태 |
|---|---|---|
| `AGENTS.md` | (이 파일) 프로젝트 컨텍스트·확정 결정 | 최신 |
| `P1_Plan_1.md` | 담당 A 7일 상세 플랜 (일자별 체크리스트·완료 기준) | 최신, D2·D11 반영 |
| `P2_Plan_1.md` | 담당 B 7일 상세 플랜 (모듈 전달 일정·실제 파일 경로 포함) | 최신, D2·D4·D5 반영 |
| `docs/감도_GAMDO_기능명세서_v1.0_3.md` | 모듈 M1~M15 기능 정의, API 계약(§10), 예외 규칙(§13) | 최신, D2·D4·D5 반영 |
| `docs/감도_GAMDO_DB스키마_v2.0.md` | 로컬 14테이블 + 서버 3테이블 DDL(SQLite 검증 완료), 동결 규약 | 최신 (v1.0은 폐기) |
| `docs/감도_GAMDO_PRD_v1.0_2.md` | 제품 요구사항 전체(정식 버전 범위 포함) | D2 반영됨. 프로토타입 범위는 D6·D11 우선 |
| `docs/감도_GAMDO_디자인_참고서_v1.0.md` | 정보 구조·레이아웃 참고서 | 참고 자료 — D11과 충돌하는 탭/홈 구조는 채택하지 않음 |
| `docs/MOODFRAME_2인_개발_로드맵.md` | 최초 2인 개발 로드맵 | 역사 자료 — D1(RN) 등 폐기된 결정 포함, 참고만 |

## 6. 문서 충돌 시 우선순위

`AGENTS.md §3 확정 결정` > `P1_Plan_1.md`/`P2_Plan_1.md`(§0 전제 표) > `docs/감도_GAMDO_기능명세서_v1.0_3.md` > `DB 스키마 v2.0`(데이터 구조에 한해 최우선) > `PRD` > 역사 자료.
구버전 서술(자동 촬영, 텍스트 안내, 일치도 게이지, RN, 서버 프로필 API, **무드 탭·홈 화면·4탭 하단바·styleExplore·myStyle**(→ D11: 카메라가 홈), **프리셋 3종**(→ D6: 6종 확정))을 발견하면 이 파일 기준으로 정정하라.

## 7. 불변 규칙 (작업 중 위반 금지)

1. **완료(Done)의 정의**: 코드 작성이 아니라 "사용자가 실기기에서 해당 흐름을 끝까지 수행 가능" + 모듈은 단위 테스트 통과.
2. **DB 스키마 동결**: 컬럼 변경·삭제 금지, 추가(additive)만 허용. 파라미터류 구조 변경은 JSON payload `v` 증가로. 얼굴 임베딩·위치 정보 영구 저장 컬럼 추가 금지.
3. **D2·D8은 재론 불가**: 텍스트 안내/자동 촬영/게이지 추가 제안, 얼굴 변형 기능 제안은 사용자(제품 오너)의 명시적 번복 없이 구현하지 않는다.
4. **Day 6부터 신규 기능 금지** — 오류·성능·데모 품질만.
5. **생성 실패를 사용자에게 노출하지 않는다** — 항상 기본 보정 결과로 폴백("자연스러운 보정만 적용했어요").
6. **더미로 속이지 않는다** — 생성 API 불안정 시 고정 이미지를 결과로 위장하지 말고 기능 플래그로 숨긴다.
7. UI 문구에 전문 용어 금지(삼분할·헤드룸·네거티브 스페이스 등), 상태 표시는 허용하되 행동 지시 문구는 금지(D2).

## 8. 현재 상태와 다음 작업

**상태 (2026-07-25 기준): 담당 A 앱 기반과 담당 B(P2) 로직·서버·GPU 경계 통합이 진행되어 있다.** 현재 `main`과 활성 P2 브랜치 `codex/p2-plan-sync`는 동일 파일 상태로 동기화되어 있고, 원격 `origin/main`은 `2b27add`까지 반영되어 있다. 실기기(SM-G970N, Android 12)에서 온보딩·카메라·앨범·보정·API 왕복·CAMP-2 결과 다운로드를 확인했다.

- **Day 1 완료(1-1~1-5):** 프로젝트 셋업 · 권한 처리 · 로컬 데이터(DeviceId·Room 14테이블·presets 시딩) · 화면 골격(t2 네비: onboarding→camera(홈)→album→result) · CameraX 프리뷰·촬영·저장(filesDir + MediaStore + captures)
- **P2 구현 완료 범위:** `FrameFeatureCalculator`·`matchScore`·`AlignmentEngine`·`ProblemDiagnoser`·`ProfileEngine`과 JVM 테스트, `guide_config.json` 파싱, 카메라 오버레이 연결, 메모리 기반 `/references/analyze`, 편집 큐 워커·EXIF 스트립·입력 즉시 purge·결과 24시간 purge 예약·7일 job 메타 purge, `GenerativeEditProvider`·ComfyUI HTTP 어댑터·LaMa 마스크/seed workflow·InsightFace 검증 fallback이 구현되어 있다. `/edit-jobs`는 동시/시간당 요청, 이미지 크기·해상도, 편집 영역 제한을 적용한다.
- **P2 로컬 데이터·운영:** 6종 `presets.json`과 v1 카드 메타 16장(`cards.json`)은 검증 스크립트를 통과했다. 레퍼런스 합성 10장 분석은 최대 약 136ms였고, `job_stats.py`·`local_metrics.py`가 서버 job 및 앱 로컬 KPI 집계 경로를 제공한다. Android JVM 테스트는 `BUILD SUCCESSFUL`, 서버 테스트는 `28 passed`다. CAMP-2의 ComfyUI LaMa smoke, 재부팅 후 실제 후보 2개 생성·InsightFace 통과·다운로드, SSH 터널 실기기 API 테스트, GPU·FastAPI·worker 자동 기동 및 실 HTTP smoke도 통과했다. 사진 살리기 드래그 마스크·후보 선택·선택 결과 저장과 얼굴 보호 마스크 fallback까지 실기기·CAMP-2에서 확인했다. 카메라 Y 평면의 전체·얼굴·배경 밝기 샘플링 입력도 연결했으며 실제 역광 장면 판정만 남아 있다.
- **앱 통합 상태:** 앱은 `app/src/main/assets/presets.json`의 6종 번들 폴백으로 오프라인 동작한다. 온보딩 선택 카드와 추천 스타일 ID를 로컬 설정에 저장하고, 카메라 가이드 프리셋과 보정 기본 필터에 반영한다. 결과 화면은 원본을 먼저 표시한 뒤 필터를 비동기로 적용하며, 반복 저장 시 편집 단계 번호를 증가시킨다.

미해결/대기: 구현한 핀치 줌의 직접 실기기 제스처 판정, 카메라 오버레이 인물 정렬·색 전환 최종 시각 판정, 실제 역광 장면의 임계값 판정, 큐레이션 사진의 정식 품질 판정·InsightFace 임계 캘리브레이션, 자동 행인 마스크·FLUX.1 Fill, 실제 카드 이미지 라이선스 확인이 필요하다. CAMP-2는 재부팅 후 커널/NVML 불일치가 해소되어 RTX 3090을 `nvidia-smi`와 PyTorch CUDA에서 정상 인식한다. 종료된 Dure 서비스·컨테이너·모델 저장소는 사용자 승인으로 제거했으며, GAMDO 서비스는 보존했다. (프리셋 종수는 6종 확정 — D6)

**빌드 방법:** `JAVA_HOME`을 Android Studio JBR(JDK 17)로 지정 후 `./gradlew :app:assembleDebug`. SDK는 `C:\android-sdk`. (환경 상세는 세션 메모리 참조)

## 9. 용어 사전

| 용어 | 의미 |
|---|---|
| StylePreset | 스타일 1개의 구도(composition)+보정(color) 파라미터 JSON |
| StyleProfile | 사용자 개인의 취향 벡터(구도/색감 분리, 차원별 확신도 포함, 로컬 저장) |
| ResolvedStyle | 프리셋 + 개인 프로필을 결합한 최종 목표 파라미터 |
| FrameFeatures | 프레임별 실시간 특징(인물 박스·여백·수평·밝기 등) |
| StyleTarget / TargetComposition | 오버레이가 그릴 목표 구도(프리셋 또는 레퍼런스 분석에서 생성) |
| OverlayState | AlignmentEngine 출력 — 목표 프레임·실루엣·수평선 좌표 + 표시 여부 |
| matchScore | 내부 일치도 점수 — **UI 표시 금지**, KPI 로그 전용 |
| Rescue / 사진 살리기 | 기존 사진 진단→보정→생성 복구 흐름 |
| EditJob | 서버 비동기 생성 편집 작업(클라이언트가 ID 생성, 1초 폴링) |
