# AGENTS.md — 감도 (GAMDO) 현재 계약

> **갱신일:** 2026-07-29
> **목적:** 새 세션의 구현·검증 기준을 이 파일 하나로 전달한다.
> **우선순위:** `remain_plan.md` §0의 최신 오너 결정 > 이 파일 > `docs/감도_데모_데이터_운영계약_2026-07-28.md` > DB 스키마 v2.0(구조에 한함) > 역사 계획·PRD. 이 파일과 `remain_plan.md`가 충돌하면 `remain_plan.md`를 따른다.

---

## 1. 현재 제품 한 줄 요약

**감도(GAMDO)**는 사용자의 취향 카드 또는 레퍼런스 사진을 바탕으로, 카메라의 시각 오버레이로 구도를 제안하고 사용자가 직접 촬영한 사진을 로컬 보정 또는 명시적 생성 복구로 완성하는 Android 카메라 앱이다.

데모에서 우선 완성할 흐름은 다음 세 가지다.

```text
1. 온보딩 → 스타일 가이드 촬영 → 로컬 보정 → 저장
2. 사진으로 만들기 → 레퍼런스 분석 → 내 레퍼런스 적용 → 촬영/보정
3. 앨범 사진 → 사진 살리기(객체 제거·여백 확장) → 결과 선택 → 저장
```

AI 2·AI 3은 명시적으로 시작한 경우에만 CAMP-2를 사용한다. 라이브 카메라 프레임은 서버로 보내지 않는다.

## 2. 목표와 역할

- **2인·7일 프로토타입**, Android/Kotlin/Compose 단일 플랫폼
- 완료 기준: 세 흐름 각각을 실기기에서 3회 연속, 크래시 없이 끝까지 수행
- P1: 화면·CameraX·Compose·로컬 편집 연결
- P2: 온디바이스 구도/검출 로직·순수 Kotlin 모듈·성능 계측 경계
- P2는 AI 2·AI 3에 필요한 `gamdo-server/**`와 CAMP-2를 수정할 수 있다. 그 밖의 서버 하드닝은 별도 우선순위다.

## 3. 확정 계약

| ID | 계약 | 현재 기준 |
|---|---|---|
| D1 | 플랫폼 | Android, Kotlin 네이티브, Jetpack Compose. iOS·RN 없음. |
| D2 | 촬영 가이드 | 시각 오버레이만 사용한다. 브래킷·인물 실루엣·수평선은 허용, 텍스트 지시·화살표·점수·슬롯 채움색·자동 촬영은 금지한다. 셔터는 항상 사용자가 누른다. |
| D3 | 자동 구도 | 카메라 진입 시 장면을 온디바이스로 탐색하고 고정 레이아웃을 제안한다. 물체 라벨은 보조 신호일 뿐이며, 중심 주요 피사체의 수·배치가 우선이다. 인식 실패 시 임의 레이아웃을 띄우지 않는다. |
| D4 | 데이터 | 로그인 없음. 프로필·선호도·촬영 세션은 로컬 Room에 저장한다. `feedback`·`consents` 테이블은 스키마 동결상 남지만 현재 writer는 없다. |
| D5 | 서버 범위 | FastAPI + CAMP-2는 AI 2의 `/references/analyze`와 AI 3의 `/edit-jobs`에 사용한다. 모두 사용자가 사진 선택·실행을 명시한 경우에만 호출하며, 라이브 카메라 분석은 서버로 보내지 않는다. |
| D6 | 스타일 | Clean Social, Candid Feed, Bright Review, Soft Film, Casual Portrait, Night Street의 6종을 유지한다. 온보딩 프로필의 전체 추천 순서를 `app_settings`에 로컬 저장하고, 스타일 스트립 앞부분에 그 순서대로 반영한다. |
| D7 | 생성형 | LaMa 객체 제거, FLUX.1 Fill 여백·화각 확장, IC-Light 재조명, ViewCrafter 제한적 시점 변경이 CAMP-2에 배치됐다. readiness가 참인 작업만 앱에 노출한다. InsightFace는 결과 품질 신호로 사용하되, 탐지기 미검출만으로 유효 결과를 숨기는 하드 게이트로 쓰지 않는다. |
| D8 | 데모 데이터 | 폐쇄형 토이프로젝트·CAMP-2 데모 환경에서는 명시적으로 고른 레퍼런스·편집 사진, 마스크, 결과 후보, 분석 로그를 품질 디버깅용으로 보관할 수 있다. EXIF 제거와 임시 파일 삭제는 최선 노력이며 처리 성공의 선행조건이 아니다. 상세 수명·정리 기준은 `감도_데모_데이터_운영계약`을 따른다. |
| D9 | 촬영 | 1인 인물 우선이다. 현재 저장 호환 비율은 4:5·1:1이지만 최종 P1 화면은 비율 칩 없이 풀블리드 프리뷰를 사용한다. 프리뷰·분석·저장 좌표는 `PreviewGeometry`로 일치시킨다. |
| D10 | 디자인 | t2 디자인을 따른다. 카메라가 홈이며, 다크 차콜·세이지·사진 중심·전문 용어 금지를 유지한다. |
| D11 | 화면 구조 | 온보딩 → 카메라(홈) → 앨범 → 결과. 무드 홈·4탭 하단바는 없다. |
| D12 | 장면·슬롯 | 유효한 중심 물체는 라벨/분할 마스크 없이도 후보가 된다. 최대 4개 슬롯, 얼굴이 있으면 인물 슬롯을 함께 둔다. 기본 ROI는 하단 중심이며, 중복·가장자리 배경·케이블성 박스는 제외한다. 앵커는 5개 새 검출 중 3회, 동반 물체는 2회 이상 관측돼야 고정한다. 확정 후 재탐색 전까지 이동하지 않는다. |
| D13 | 레이아웃 제어 | 기본은 자동 탐색이다. 재탐색은 고정을 해제하며, 탭은 해당 지점 주변을 다시 찾는다. 사용자는 프레임 버튼에서 12종 수동 프레임을 선택할 수 있어야 한다. 수동 선택은 즉시 고정되고 세션 간 보존하지 않는다. P2 API는 완료됐고 P1 패널 연결이 남아 있다. |
| D14 | 촬영 후 처리 | 촬영·앨범 사진은 사용자의 현재 감도(시스템 추천 또는 내 감도) 로컬 보정 결과를 기본으로 먼저 제시하고, 원본·다른 필터는 비교 선택지로 둔다. `사진 살리기`는 2차 보조 기능이며, 사용자가 화각/여백·시점·조명 의도를 선택해 명시적으로 실행할 때만 CAMP-2를 요청한다. 레퍼런스 색감과 생성 결과는 원본과 별도 결과로 저장한다. |
| D15 | 품질 | 지속 장면의 첫 레이아웃은 2초 이내, 객체 분석은 평균 8FPS 이상이 목표다. 현재 최신 통합본의 실기기 수치는 아직 재측정 전이므로, 과거 수치로 완료 판정하지 않는다. |
| D16 | 처리 위치 | 실시간 얼굴·물체·레이아웃은 온디바이스다. 카메라 프레임을 실시간 서버로 올리지 않는다. Android 포즈 추론은 폐기했다. |
| D17 | 탐색 표시 | 탐색 중에는 중앙의 작고 비텍스트 로딩 표시만 보이고, 고정 뒤에는 사라진다. 레이아웃이 없어도 촬영은 가능하다. |
| D18 | 영역 선택 | 드래그 탐색은 상시 제스처가 아니다. 사용자가 `영역 선택` 버튼을 눌러 모드에 진입한 뒤 그린 닫힌 경로만 다각형 ROI로 사용한다. 취소하면 기본 자동 탐색으로 돌아간다. 우선순위는 `REFERENCE > MANUAL > POLYGON > TAP > DEFAULT`다. |
| D19 | 생성 보정 제어 | 보는 위치·화각·여백·조명 생성은 AI가 임의로 결정하지 않는다. 사용자가 방향·범위·강도를 고르고 실행해야 한다. 서버 capability가 참이어도 실기기 성공·품질 기준이 확인되지 않은 작업은 노출하지 않는다. |

## 4. 활성 아키텍처

```text
CameraX Preview / Analysis / Capture
  → ML Kit 얼굴 + EfficientDet 객체 검출
  → StableSceneTracker(기본·탭·다각형 ROI, 중심 피사체 그룹·3/5 안정화)
  → SceneGuideSessionController(자동·12종 수동·영역·레퍼런스 고정 상태)
  → CameraOverlay(브래킷·실루엣·수평선·탐색 로딩)
  → Capture → LocalEditor(수평·광학 보정 + 스타일) → MediaStore 저장
  → [명시적 선택] Reference Analyze / Edit Job → CAMP-2 → 결과 선택 → MediaStore 저장
```

- EfficientDet은 GPU를 먼저 시도하고 CPU로 폴백하며, 선택된 delegate와 강등 원인을 로그로 남긴다.
- EfficientDet의 이전 ML Kit 객체 검출 폴백은 성능 계약상 제거됐다. 검출기를 만들지 못하면 정직하게 빈 객체 결과를 낸다.
- 프리셋·카드 에셋은 번들 폴백을 유지한다. 서버 호출은 AI 2·AI 3의 명시적 사용자 실행에 한정한다.
- 모든 튜닝 값은 `app/src/main/assets/guide_config.json`에 둔다.

## 5. 문서 상태

| 파일 | 용도 | 상태 |
|---|---|---|
| `remain_plan.md` | 최신 오너 결정·잔여 작업·실기기 체크 | 최상위 실행 계획 |
| `AGENTS.md` | 현재 에이전트 계약 | 최신 |
| `docs/감도_계약변경_2026-07-28.md` | 이번 P1 변경과 구현 준수 감사 | 최신 |
| `docs/감도_데모_데이터_운영계약_2026-07-28.md` | 폐쇄 데모용 업로드·보관·검증 기준 | 최신 |
| `docs/AI2_레퍼런스_통합_계약_2026-07-28.md` | AI 2 레퍼런스 분석·내 감도 계약 | 최신 |
| `docs/provider_decision.md` | AI 3 생성 공급자·품질 게이트 결정 | 최신 |
| `docs/P1_P2_카메라UX_AI개선_요구사항_2026-07-29.md` | 풀블리드·필터/프레임 패널·영역 선택 버튼·인물 브래킷·생성 복구 P1/P2 연결 | 최신 통합 요청 |
| `docs/감도_전체기능_구현상태_2026-07-29.md` | 전체 사용자 기능·코어 구현·P1 연결·GPU 배치·실기기 체크리스트 | 최신 구현 현황 |
| `P1_Plan_1.md` | P1 구현 이력 | 부분 무효, 첫머리 고지 참조 |
| `P2_Plan_1.md` | P2 초기 로드맵·서버 자산 이력 | 부분 무효, 첫머리 고지 참조 |
| `docs/감도_GAMDO_DB스키마_v2.0.md` | Room/서버 DDL | 구조 동결 기준 |
| PRD·기능명세서·초기 로드맵 | 제품/초기 설계 이력 | 현 범위와 충돌 시 채택하지 않음 |

## 6. 불변 작업 규칙

1. 완료는 코드·JVM 통과가 아니라 실기기 단일 흐름 성공이다.
2. Room 스키마는 변경하지 않는다. 단, CAMP-2 데모 작업공간의 입력·결과·품질 진단 보관은 허용한다.
3. D2의 금지 항목과 얼굴 변형 금지는 오너의 명시적 번복 없이는 재논의하지 않는다.
4. 새 기능보다 오류·성능·시연 품질을 우선한다.
5. AI 2·AI 3의 명시적 사용자 진입점은 다시 연결할 수 있다. 실시간 카메라 프레임의 무단 업로드·공개 공유·계정 기능은 추가하지 않는다.
6. 사용자에게 고정 이미지·더미 결과를 실제 처리 결과로 보이지 않는다.
7. P1 소유 화면/디자인을 변경하기 전에는 사용자 확인을 받는다. P2는 연결 계약과 순수 로직을 우선한다.

## 7. 현재 구현·검증 상태 (2026-07-29)

- **구도:** 중심 피사체 그룹 선택, 3/5 안정화, 동반 물체 2/5 누적, 최대 4슬롯, 재탐색, 전후면 좌표 매핑, 탐색 로딩이 구현됐다.
- **성능 경로:** 분석 스레드 변경은 커맨드 큐로 직렬화됐고, 콜드 스타트에서 검출기 생성이 분석 스레드로 이동했다. delegate 선택은 관측 가능하다.
- **로컬 편집:** 결과 진입 시 자동 수평·광학 보정, 스타일 필터·저장 렌더 경로가 구현됐다.
- **개인화:** 카드 로딩 단일화, 프로필 저장, 추천 스타일 스트립 정렬, 카드 선택 ID 형식이 구현됐다.
- **가이드 확장:** 풀블리드 좌표, 12종 수동 프레임, 버튼 진입형 다각형 ROI, 얼굴·사람 박스 기반 고정 인물 브래킷의 P2 코어가 구현됐다. Android ML Kit Pose와 점/막대 렌더링은 폐기했으며 P1의 버튼·패널·제스처·브래킷 렌더링이 남아 있다.
- **AI 2:** CAMP-2 v3 분석·Photo Picker·동의/미리보기·로컬 해시 캐시·레퍼런스 구도/색감·오버레이가 연결됐다. 필터 소실 방지 상태 홀더의 ResultScreen 단일 원천 연결과 실기기 5회 판정이 남아 있다.
- **AI 3:** LaMa·FLUX.1 Fill·IC-Light·ViewCrafter가 CAMP-2에 배치됐고 공개 edit-job 결과를 확인했다. 최신 앱에서 추천→실행→후보 비교→저장을 작업별로 재검증해야 한다.
- **자동 검증:** 최신 통합 브랜치에서 JVM 전체 테스트와 Debug APK 빌드가 통과했다.
- **실기기 재검증 필요:** EfficientDet delegate/객체 인식률·8FPS·첫 레이아웃 2초·재탐색·셔터→저장→앨범 왕복은 최신 P1 병합 이후 다시 확인해야 한다.

## 8. 용어

| 용어 | 의미 |
|---|---|
| StylePreset | 6종 시스템 스타일의 구도·색감 파라미터 |
| StyleProfile | 온보딩 카드에서 만든 로컬 취향 벡터 |
| StyleTarget | 현재 스타일이 오버레이에 요구하는 목표 구도 |
| SceneInterestRegion | 자동 탐색이 우선하는 중심 피사체 영역 |
| StableSceneTracker | 새 검출을 누적해 고정할 피사체 그룹을 결정하는 온디바이스 로직 |
| 재탐색 | 현재 자동 레이아웃 고정을 해제하고 현 장면을 다시 분석하는 동작 |

## 9. 2026-07-30 상황 우선 가이드 V2 교체 계약

현재 객체 수 중심 자동 템플릿을 즉시 삭제하지는 않지만, 새 카메라 계약의 P2 공개 경계는
`guide/SceneModeGuide.kt`를 기준으로 한다.

- `CaptureSceneMode`는 `AUTO`, `PORTRAIT`, `ENVIRONMENTAL_PORTRAIT`, `CAFE_FOOD`,
  `TRAVEL_LANDSCAPE`, `STILL_LIFE`를 제공한다.
- `SceneGuideSessionController.sceneModeDecision`은 Auto 제안 또는 사용자 선택을 전달한다.
  사용자 선택은 `USER` 출처로 고정되며 재탐색이 덮어쓰지 않는다. Auto 신뢰도가 0.65 미만이면
  임의 모드를 확정하지 않는다.
- `guideMarks`는 `GuideMark.SubjectDot`, `GuideMark.PersonSilhouette`,
  `GuideMark.HorizonLine`만 노출한다. raw detector box, 라벨, confidence, track ID와 슬롯
  채움 상태는 P1 화면으로 전달·표시하지 않는다.
- 객체 점의 개수는 선택된 안정 객체와 1:1이며, 좌표는 현재 물체를 추적하지 않는 고정 템플릿에서
  나온다. 인물은 얼굴/person box 기반 고정 실루엣을 사용한다.
- `sceneGuide` 설정의 classifier/instance-segmenter asset이 null이면 해당 모델은 준비되지 않은
  상태다. MobileNetV3 장면 분류기와 YOLO11n-seg LiteRT를 번들하기 전까지는 모델이 준비됐다고
  주장하지 않고 보수적 휴리스틱 또는 수동 프레임으로 축소한다.
- P1은 모드 선택 UI와 점·링·실루엣 렌더링만 연결한다. P2는 `SceneTechniqueSelector`와
  모델 readiness를 소유하며 화면 파일을 수정하지 않는다.
