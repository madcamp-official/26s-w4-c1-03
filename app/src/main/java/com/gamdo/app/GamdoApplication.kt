package com.gamdo.app

import android.app.Application

/**
 * Application entry point. Kept intentionally minimal on Day 1 — DI/DB wiring
 * (Room, DeviceId, Repository) lands with §1-3.
 */
class GamdoApplication : Application()
