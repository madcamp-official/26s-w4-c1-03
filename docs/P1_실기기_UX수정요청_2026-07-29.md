# P1 실기기 UX 수정 요청 — 온보딩부터 촬영·보정까지

작성일: 2026-07-29  
기준 기기: SM-S928N  
검증 범위: 앱 데이터 삭제 → 권한 → 온보딩 → 카메라 → 앨범 → 내 감도 → AI 보정 → 재실행 → 셔터  
담당: P1 화면·CameraX·Compose·내비게이션

## 1. 목적과 판정 기준

이 문서는 `docs/감도_전체기능_구현상태_2026-07-29.md`를 실사용자 흐름으로 다시 검증한 결과 중 P1이 수정하거나 연결해야 하는 항목만 정리한다. P2의 상태·탐지·서버 계약은 새로 재구현하지 않고 기존 공개 API를 화면에 정확히 연결한다.

완료는 화면이 존재하는지가 아니라 사용자가 다음 흐름을 설명 없이 끝까지 수행할 수 있는지를 기준으로 한다.

```text
처음 실행 → 취향 저장 → 촬영 → 앨범 → 내 감도 자동 보정 확인
→ 필요할 때만 AI 복구 → 결과 비교 → 새 파일 저장
```

## 2. 최우선 블로커

### P1-B1. 셔터 무한 로딩과 촬영 완료 콜백

초기 검증에서는 셔터가 `takePictureInternal` 뒤 완료되지 않는 사례가 있었다. 다만 2026-07-29 최신 `main`을 원래 기준 기기 `SM-S928N`에서 재검증한 결과, 수동 촬영 → MediaStore 저장 → 앨범 → 보정 화면까지 약 1.95초에 완료됐다. 이 항목은 **해결 확인**으로 전환하되, 연속 촬영·실패 복구 회귀는 유지한다.

수정 요구:

- 셔터 요청을 중복 방지하는 단일 상태로 관리한다.
- 성공 시 저장 완료 후 최근 촬영 썸네일과 Room 기록을 갱신한다.
- 실패·타임아웃 시 촬영 상태를 반드시 해제해 다시 촬영할 수 있게 한다.
- 카메라 재바인딩과 촬영 callback lifecycle이 충돌하지 않는지 확인한다.
- 처리 중 사용자가 화면을 이탈하거나 앱이 background로 가는 경우 상태를 정리한다.
- 성공·실패·타임아웃을 Debug 로그로 구분하되 제품 화면에는 기술 오류를 노출하지 않는다.

완료 기준:

- 데이터 초기화 직후부터 촬영 5회가 모두 5초 이내 저장 또는 안전 복구된다.
- 한 번의 실패가 이후 촬영을 막지 않는다.

### P1-B2. 내 감도 적용 결과가 화면에서 사라짐

서버 분석과 P2 저장 로그는 성공했지만 완료 시트를 닫은 뒤 결과 화면에는 다시 `+ 내 감도`만 보였다. 생성한 `내 감도` 항목과 적용 결과는 보이지 않았다.

P1 연결 요구:

- 결과 화면은 일시적인 생성 시트 상태가 아니라 P2의 영속 `ResultFilterState`를 구독한다.
- `Applied → 시트 닫기`가 필터 목록을 초기화하지 않게 한다.
- 기본 6종은 항상 유지하고, 색감 capability가 있는 활성 레퍼런스를 일곱 번째 항목으로 표시한다.
- 필터 선택은 `selectedId`만 변경하고 목록 자체를 재생성·숨김 처리하지 않는다.
- 미리보기 렌더 실패 시 현재 사진의 원본을 표시하고 필터 목록은 유지한다.
- 앱 재구성·앨범 재진입·뒤로가기 후에도 활성 레퍼런스를 다시 표시한다.

P2가 활성 레퍼런스 상태를 정상적으로 내보내지 않는 경우에는 `docs/P2_실기기_기능수정기록_2026-07-29.md`의 P2-B2로 전달한다.

### P1-B3. 온보딩 약속과 결과 화면 기본값 불일치

온보딩은 “앞으로 촬영 가이드와 보정에 이 느낌을 자동으로 반영해요”라고 안내하지만, 사진을 열면 `원본`이 기본 선택된다.

수정 요구:

- 사진을 열었을 때 먼저 현재 감도의 로컬 보정 결과를 보여준다.
- 원본은 즉시 비교 가능한 항목으로 유지한다.
- `내 감도 만들기`와 `현재 내 감도 적용`을 동일한 `+ 내 감도` 문구로 표현하지 않는다.
- 활성 감도 이름 또는 대표 팔레트를 표시해 무엇이 적용됐는지 알 수 있게 한다.
- P2가 제공하는 자동 보정 결과가 없을 때만 원본을 안전 기본값으로 사용한다.

## 3. 온보딩과 권한

### P1-O1. 권한 요청 전 제품 가치 설명

- 현재 첫 화면은 큰 빈 공간과 권한 설명만 있어 사용자가 왜 권한을 주어야 하는지 알기 어렵다.
- “좋아하는 사진을 고르면 촬영 구도와 보정에 반영된다”는 핵심 흐름을 권한 요청 전에 짧게 보여준다.
- 전체 사진 접근 권한은 앱 실행 직후가 아니라 앨범 진입 시 요청하는 방식을 우선 검토한다.
- Photo Picker로 처리 가능한 레퍼런스 선택에는 전체 사진 권한을 요구하지 않는다.

### P1-O2. 취향 선택의 의미와 카드 접근성

- 카드 선택이 구도·색감·분위기 중 무엇을 결정하는지 한 문장으로 설명한다.
- 인물 사진에 편중되지 않도록 카드 배치는 P1 큐레이션 기준에서 균형을 맞춘다.
- 저장 전에 “이 선택으로 달라지는 것”을 구도·색감 미리보기로 연결한다.
- 모든 카드와 아이콘 버튼에 의미 있는 `contentDescription`을 제공한다.
- 선택 0개 시작과 최소 5장 조건은 유지한다.

## 4. 카메라 화면

### P1-C1. 제품용 화면과 Debug HUD 분리

- 앱 데이터 초기화 뒤에도 HUD가 기본 노출됐다. Debug 빌드라도 기본값은 숨김이어야 한다.
- HUD는 명시적인 개발자 동작으로만 켜고 제품 시연 경로에서는 유지되지 않게 한다.
- 원시 객체 수·FPS·IoU·match·fixed 같은 수치는 제품 화면에 표시하지 않는다.

### P1-C2. 풀블리드 촬영 화면과 접히는 패널

- 상단 `4:5/1:1` 칩을 제거하고 프리뷰를 안전 영역까지 풀블리드로 확장한다.
- 하단 6종 필터 스트립은 항상 보이지 않고 필터 버튼을 눌렀을 때만 패널로 연다.
- 필터 패널을 닫으면 셔터와 피사체가 중심이 되는 일반 카메라 화면으로 돌아간다.
- `내 감도 적용 중`은 어떤 감도와 어떤 범위가 적용되는지 최소 정보만 제공한다.
- `docs/P1_P2_카메라UX_AI개선_요구사항_2026-07-29.md`의 SNOW·갤럭시 카메라 참고 계약을 따른다.

### P1-C3. 탐색·고정 상태 렌더링

실측 상태가 `물체 0`, `fixed=none`인데 큰 사각 가이드가 표시됐다.

- P2 상태가 `Searching`이면 작은 비텍스트 로딩만 표시한다.
- `Fixed`일 때만 전달받은 고정 슬롯·포즈 가이드를 그린다.
- `fixed=none` 상태에서 임의 사각 프레임을 표시하지 않는다.
- 로딩은 화면 중앙을 가리지 않는 작은 위치에 둔다.
- 슬롯 채움 상태·색 전환·행동 지시 문구는 추가하지 않는다.

### P1-C4. P2 기능의 사용자 진입점 연결

다음 P2 코어가 구현돼 있으나 실제 화면에서 접근할 수 없었다.

- 영역 선택 버튼 → 드래그 경로 → `rescanInPolygon()`
- 프레임 버튼 → 12종 프레임 패널 → `selectManualLayout()`
- 자동으로 돌아가기/재탐색 → `rescan()`
- 인물 포즈 템플릿 → 고정 점·막대 렌더링
- 내 레퍼런스 원본 오버레이 → 투명도임을 알 수 있는 버튼과 0~60% 조절

## 5. 앨범과 결과 편집

### P1-R1. 앨범 정보 구조

- 앱 촬영 사진과 기기 사진을 구분하거나 필터링할 수 있게 한다.
- 최소한 날짜 그룹 또는 `감도로 찍은 사진` 진입점을 제공한다.
- 사진 셀에 접근성 설명을 제공한다.
- 사진 선택 즉시 결과 화면으로 이동하더라도 현재 선택 목적을 상단에서 알 수 있게 한다.

### P1-R2. 현재 사진 기반 필터 미리보기

- 결과 화면의 필터 썸네일은 고정 인물 사진이 아니라 현재 편집 중인 사진으로 렌더링한다.
- 미리보기 생성 중에는 원본 썸네일과 로딩을 사용하고 목록을 숨기지 않는다.
- 자주 쓰는 `원본 / 내 감도 / 다른 감도`를 우선 노출하고 세부 수동 조절은 접을 수 있게 한다.
- 밝기·온도·대비 등 다수의 조절값이 기본 화면을 압도하지 않게 고급 조절 패널로 정리한다.

## 6. AI 보정 화면

### P1-A1. 사용자의 감도 자동 보정을 첫 결과로 표시

현재 AI 보정은 “지울 수 있는 것들을 찾아볼게요”로 시작해 제품 의도와 다르다.

권장 흐름:

```text
AI로 보정
→ 내 감도 자동 보정 미리보기
→ 사진 상태가 나쁠 때만 화각·시점·조명 추천
→ 사용자가 실행
→ 원본/감도/AI 후보 비교
```

- 객체 삭제는 기본 진입 설명에서 제외하고 `직접 수정 → 지우기`로 둔다.
- 서버 진단 결과가 `local_style`뿐이면 별도 업로드 흐름을 반복하지 말고 로컬 결과를 바로 보여준다.
- 생성 작업이 추천되지 않아도 직접 수정에서 서버 capability가 준비된 작업을 선택할 수 있게 한다.
- 시점 변경은 방향·강도를 사용자가 확인할 수 있어야 한다.
- 추천이 없을 때 “이대로 두기”만 보여주지 말고 원본/감도 비교와 저장을 유지한다.

### P1-A2. 처리와 결과 피드백

- 분석·생성·다운로드 상태를 한 개의 비차단 진행 UI로 표현한다.
- 실행 전에는 서버 작업이 발생하지 않는다는 계약을 유지한다.
- 결과가 생기면 원본·내 감도·AI 후보를 같은 비교 화면에 유지한다.
- 생성 실패 시 기술 오류 대신 원본과 로컬 감도 결과를 유지한다.

## 7. P1 완료 체크리스트

- [ ] 셔터 5회 연속 저장과 타임아웃 복구
- [ ] 온보딩 완료 뒤 사진을 열면 내 감도 결과가 기본 표시
- [ ] 내 감도 생성 후 필터 6종 + 내 감도 유지
- [ ] 데이터 초기화 뒤 HUD 기본 숨김
- [ ] 객체 0·`fixed=none`에서 큰 프레임 미표시
- [ ] 풀블리드 프리뷰와 접히는 필터 패널
- [ ] 영역 선택·12종 프레임·재탐색 진입점 연결
- [ ] 인물 포즈 점·막대 렌더링
- [ ] 현재 사진 기반 필터 미리보기
- [ ] AI 보정이 내 감도 결과부터 시작
- [ ] 생성 capability에 맞는 직접 수정 경로 제공
- [ ] 온보딩 카드·앨범 셀·아이콘 접근성 설명

세부 시각 표현은 P1이 감도 브랜드에 맞게 완성하되, 테스트 HUD와 임시 화면을 최종 디자인으로 간주하지 않는다.

## 8. 추가 구현분 연결 요청 — 상황별 감도·공유 촬영·비교 시연

아래는 `codex/p2-profile-rescue-share`에서 P2 코어·서버 계약까지 구현됐지만, P1 화면에 아직
진입점이 없는 기능이다. 기존 카메라·앨범의 일반 디자인 변경과 달리, **새 기능을 실제 사용자
흐름으로 완성하기 위해 필요한 최소 화면 연결**을 요청한다.

### P1-N1. 상황별 감도 프로필 정교화

사용자는 처음 카드 5장 이상으로 기본 감도를 저장한 뒤, 선택적으로 취향을 더 정교화할 수 있어야
한다. 복잡한 설정 화면을 노출하는 대신 다음 두 진입점을 제공한다.

```text
내 감도 관리
→ 취향 더 고르기 (적응형 카드 묶음)
또는
→ 좋아하는 사진 추가 (최대 20장)
→ 현재 감도에 반영
```

- 적응형 카드는 첫 12장, 추가 12장, 선택적 마지막 최대 8장으로 표시한다. 사용자는 매 묶음을
  강제로 끝까지 보지 않아도 되며, 5장 이상이면 언제든 저장할 수 있다.
- 사진 추가는 시스템 Photo Picker를 사용하고, 한 번에 최대 20장을 선택한다.
- P1은 `ProfileRefinementRepository.refineFromPhotos(context, uris)`를 호출하고, 처리 중·완료·재시도
  상태를 제공한다. 사진별 분석 수치나 신뢰도는 화면에 노출하지 않는다.
- 맥락은 기술 용어가 아니라 `야간 인물`, `카페`, `여럿`, `풍경` 같은 사용자 언어로만 요약한다.
- 카드 메타데이터·이미지 제공 계약은 `docs/P1_감도프로필_카드데이터_요청.md`를 따른다.

### P1-N2. 현재 내 감도 자동 결과와 재해석 수준

앨범 사진을 열었을 때의 첫 결과는 원본이 아니라 **현재 내 감도의 로컬 보정 결과**여야 한다.
그 뒤 사진 살리기는 한 화면에서 아래처럼 단계적으로 제공한다.

```text
현재 감도 결과 / 원본 비교
→ 기억 보존 · 감도 적용 · 다시 찍었다면 중 하나 선택
→ 가능한 생성 작업만 제안
→ 사용자가 방향·강도를 선택하고 실행
→ 원본·현재 감도·AI 후보 비교 및 새 파일 저장
```

- `MEMORY`(기억 보존)는 서버 job 없이 로컬 결과를 즉시 표시한다.
- `GAMDO`(감도 적용)와 `REIMAGINE`(다시 찍었다면)은 선택과 실행 전까지 업로드·edit-job을 만들지 않는다.
- `RescueController.analyze(..., RescueContext)`에 현재 `GamdoProfileV2`의 상황별 정책과 선택한
  `ReinterpretationLevel`을 넘긴다.
- 화각/여백은 방향과 5·10·15%, 시점은 방향과 약한/기본 강도, 조명은 의도를 사용자가 고른다.
  선택하지 않은 시점·조명 작업을 임의 실행하지 않는다.
- 서버 capability가 false이거나 해당 기기에서 품질 검증을 통과하지 못한 작업은 표시하지 않는다.
- `ResultFilterStateHolder.state`를 결과 필터 목록의 유일한 원천으로 사용한다. 이 요구는 기존
  P1-B2를 대체·강화하며, 필터 Bitmap 실패가 목록을 숨기는 조건이 되면 안 된다.

### P1-N3. “나 찍어줘” QR 공유 촬영

감도 사용자가 친구에게 자신의 촬영 정책을 전달하는 새 흐름이다. 계정·새 메인 탭은 만들지 않고,
카메라 또는 내 감도 관리의 보조 진입점으로 둔다.

```text
나 찍어줘
→ 임시 촬영 세션 생성
→ QR 표시·공유
→ 친구의 모바일 웹 촬영(최대 5장)
→ 앱에서 도착 사진 확인
→ 사진 받기·내 감도 결과로 열기
```

- 세션 생성은 `ShootSessionRepository.create(policy)`를 사용하고, 반환된 절대 `shareUrl`을
  `ShootQrCode.encode(url)`로 QR 이미지로 표시한다.
- 만료까지 1시간·최대 5장이라는 사실과, 사진이 도착했을 때만 받을 수 있다는 상태를 짧게 보여준다.
- P1은 서버의 `getShootSession(sessionId, ownerToken)` 결과를 2초 간격으로 갱신해 사진 수를 보여주고,
  사용자가 `receiveAndClaim()`을 누르면 받은 사진을 기존 기기 사진 결과 흐름으로 연다. 받기/만료/
  취소 뒤에는 세션 상태를 정리한다. P2의 `ShootSessionRepository.active()`는 저장된 세션을 읽는
  함수였으나, P2가 `refresh()`와 `snapshot: StateFlow` polling facade를 추가했다. P1은 이 공개 상태를
  2초 간격으로 갱신·렌더링하면 된다.
- QR에는 전체 프로필이나 사진을 표시하지 않는다. 웹 촬영자는 앱 설치 없이 브라우저에서 촬영한다.
- 실제 웹 URL은 서버가 `/shoot/{token}`으로 제공한다. 배포 전에는 QR 진입점을 숨긴다.

### P1-N4. 같은 장면, 다른 감도 — 데모 비교

- 별도 메인 탭을 만들지 않는다. 결과 화면 또는 내 감도 관리에서 보조 액션으로 제공한다.
- 현재 사진·현재 프로필과 `GamdoComparisonEngine.compare(profile, context)`의 대비 데모 프로필을
  나란히 보여준다. 이 비교는 사용자의 프로필을 덮어쓰거나 저장하지 않는다.
- 데모 감도와 아카이브의 샘플 데이터에는 화면에서 항상 `DEMO`임을 명시한다. 고정 샘플을 실시간
  AI 결과처럼 표현하지 않는다.

### P1-N5. 새 기능 상태별 화면 책임

| 기능 | P2 제공 코드 | P1 화면 책임 |
|---|---|---|
| 상황별 프로필 | `GamdoProfileV2`, `ProfileRefinementRepository`, `CardRecommendationEngine` | 카드 묶음·최대 20장 Picker·완료/재시도 상태 |
| 기본 결과 필터 | `ResultFilterStateHolder`, `ReferenceRepository` | 단일 StateFlow 구독·현재 감도 기본 선택·목록 유지 |
| 재해석 | `RescueContext`, `ReinterpretationLevel`, `RescueController` | 3단계 선택·파라미터 선택·실행/폴링/비교 |
| QR 촬영 | `ShootSessionRepository`, `ShootQrCode` | QR·만료/도착/받기 상태·수신 사진 결과 화면 연결 |
| 데모 비교 | `GamdoComparisonEngine`, `DemoContrastProfile` | 비교 진입점·DEMO 배지·현재 사진 렌더링 |

## 9. P1 추가 체크리스트

- [ ] 적응형 카드 묶음과 최대 20장 사진 기반 프로필 정교화 진입점
- [ ] 현재 내 감도 결과를 사진 열기 기본값으로 적용
- [ ] MEMORY/GAMDO/REIMAGINE 선택과 작업별 파라미터 확인 UI
- [ ] QR 생성·도착 사진 폴링·수신/claim 흐름
- [ ] 같은 장면·다른 감도 비교의 DEMO 표기
- [ ] 배포되지 않은 QR 웹·GPU capability는 제품 화면에서 숨김
