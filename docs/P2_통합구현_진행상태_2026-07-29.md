# P2 안정화·상황별 감도·공유 촬영 통합 상태

## 이번 브랜치

`codex/p2-profile-rescue-share`

## 구현 완료

- 빈 장면은 `Searching`만 유지하며 실제 슬롯 없는 `Fixed`를 만들지 않는다. 탐색 중 큰 임의 브래킷은 숨긴다.
- 활성 레퍼런스와 필터 렌더 상태를 분리해 기존 필터와 `내 감도` 목록이 렌더 실패로 사라지지 않게 했다.
- `GamdoProfileV2`를 기존 `style_profile.subject_prefs_json`에 저장한다. 상황별 정책, 근거, 확신도를 갖고 구버전 필드는 계속 갱신한다.
- 적응형 카드 엔진(12 → 12 → 최대 8, 총 32장)과 최대 20장·동시 2개 사진 분석 기반 프로필 정교화를 구현했다.
- AI 3 분석 API는 `MEMORY/GAMDO/REIMAGINE`, 상황, 현재 감도 정책을 받는다. MEMORY는 생성 job 없이 로컬 결과만 제공한다.
- QR 공유 촬영 서버 세션(서명 토큰, 1시간, 최대 5장, GPS EXIF 제거, 앱 소유 토큰, claim/만료 purge)을 구현했다.
- Android는 공유 세션 생성·폴링·다운로드·claim transport를 제공한다.
- `gamdo-web`은 `/shoot/{token}` 브라우저 카메라와 `/archive` 데모 감도 아카이브를 제공한다. 아카이브 데이터는 화면에서 `DEMO`로 명시한다.

## 검증 완료

- Android JVM 테스트 통과.
- 서버 전체: `84 passed, 1 skipped`.
- 웹: `npm run typecheck`, `npm run build` 통과.

## P1 연결 대기

- 적응형 카드 묶음·최대 20장 Photo Picker 진입 UI.
- 재해석 수준(MEMORY/GAMDO/REIMAGINE) 선택과 현재 감도 자동 결과 비교 UI.
- QR 표시·공유 세션 사진 수신 화면.
- 필터 기본값 우선순위(`내 감도 → 세션 → 온보딩 → 원본`)를 ResultScreen에서 구독.

## 운영·실기기 대기

- CAMP-2에 현재 main 배포와 `npm install && npm run build` 후 웹 정적 번들 배치.
- FLUX·IC-Light·ViewCrafter readiness에 따라 AI 3 capability가 실제 노출되는지 검증.
- QR을 다른 HTTPS 기기에서 열어 브라우저 촬영 3장 → 앱 수신 → 현재 감도 보정까지 확인.
