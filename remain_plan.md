# remain_plan — P1 잔여 작업 플랜 (에이전트 팀용)

> **작성일**: 2026-07-28
> **기반 문서(우선순위 순)**: 본 문서 §0의 오너 결정 → `review_report.md`(2026-07-27) → `AGENTS.md` → `docs/감도_GAMDO_DB스키마_v2.0.md` → `P1_Plan_1.md` → `.claude/TEAM.md`
> **효력**: 이 문서가 `P1_Plan_1.md`·`P2_Plan_1.md`의 체크박스와 충돌하면 **이 문서가 최신**이다. 아래 O-8(2026-07-28)이 이전의 AI 2·AI 3 컷 결정을 명시적으로 대체한다.
> **최종 산출물(재정의)**: 실기기에서 기본 촬영 흐름과 AI 2(내 감도 만들기), AI 3(사진 살리기)를 각각 끝까지 수행할 수 있는 Android 앱.

---

## 0. 오너 결정 기록 (2026-07-28 — 재론 불가)

에이전트는 아래 결정을 재론하지 않는다. 결정과 충돌하는 코드·문서·플랜 서술을 발견하면 **고치지 말고 리드에게 보고**한다.

| # | 결정 | 내용 |
|---|---|---|
| O-1 | ~~시연 시나리오 A·B 둘 다 컷~~ | **O-8로 대체됨.** A(레퍼런스 따라 찍기)·B(사진 살리기)의 과거 컷 결정과 진입점 신설 금지는 더 이상 효력이 없다. |
| O-2 | **결과 화면 비디자인 항목 중 기하·광학 자동 보정만 구현** | §4-1의 수평 회전·자동 노출 보정을 사진을 열 때 **화면 변화 없이 자동 적용**한다. 전후 슬라이더(§4-2)·피드백 시트(§6-3)는 컷. 진단 칩(§4-3)·마스크 드래그는 2026-07-27에 이미 제거 확정 |
| O-3 | **자동 고정 레이아웃은 "수리만, 버튼 없이"** | `FixedLayoutGuide`/`AutoLayoutTemplateResolver` 경로는 제거하지 않고 결함(리뷰 #1·#10·#16·#17·#20 등)을 개별 수정한다. 래치는 스타일 변경 시 해제되게 한다. **D13의 레이아웃 버튼+재탐색은 만들지 않는다** — D13 위반 상태가 지속됨을 이 문서에 명시 기록한다 |
| O-4 | ~~gamdo-server는 이번 범위에서 제외~~ | **O-8로 부분 대체됨.** AI 2 분석·AI 3 생성 및 그에 필요한 서버 결함은 수정 가능하다. 인증·다중 사용자·공개 배포 하드닝은 여전히 범위 밖이다. |
| O-5 | **O-3의 "D13 버튼 없음"을 번복 — 수동 재탐색 버튼을 만든다** | 위치는 **프리뷰 우측 하단, 배율 스톱과 같은 줄**. 오너가 기기에서 증상을 직접 보고 내린 결정이다: 공존 수정으로 프리셋 가이드는 살아났지만 래치가 세션 내내 고정돼 `auto_2_row` 슬롯이 모든 장면에 유령처럼 남았다. O-3은 증상을 보기 **전**의 판단이었다. 자동 재무장은 채택하지 않는다 — 사용자가 명시적으로 요청할 때만 다시 탐색한다 |
| O-6 | **EfficientDet의 ML Kit 폴백을 버린다** | "옛날거를 그냥 버려"(2026-07-28). `EfficientDetSceneDetector`가 중앙에 물체가 없으면 `MlKitObjectDetector`를 **추가로** 돌렸는데, 빈 벽·천장처럼 흔한 장면에서 그 조건이 항상 참이라 두 검출기가 매 프레임 다 돌았다(기기 로그: 분석 39프레임에 ML Kit 39회). **B 모듈 수정 예외 승인** — R2의 예외 목록에 추가된다 |
| O-7 | **M1 브래킷 폭 수정 승인** | "M1 승인할게"(2026-07-28). `targetAspectRatio`(픽셀 비)를 정규화 폭에 그대로 곱해 4:3 분석 프레임에서 모든 브래킷이 선언 폭의 75%로 그려지던 것을 고친다. **B 모듈 수정 예외 승인** — `guide/AlignmentEngine.kt`. 계산은 `SceneLayoutGuide`와 공유하도록 `CompositionFrame`으로 뽑아 두 곳이 갈라지지 못하게 한다 |
| O-8 | **AI 2·AI 3 재개 + 데모 데이터 정책 완화** | O-1의 레퍼런스·사진 살리기 컷과 O-4의 서버 무접촉은 AI 2·AI 3 범위에서 철회한다. 사용자가 명시적으로 선택한 사진은 CAMP-2 분석·생성·품질 디버깅에 사용하고, 입력·마스크·결과·분석 로그를 수동 정리 전까지 보관할 수 있다. EXIF 제거·즉시 삭제·얼굴 검증은 품질 보조이지 흐름 차단 조건이 아니다. 라이브 카메라 프레임 업로드·공개 공유·계정은 계속 만들지 않는다. 상세는 `docs/감도_데모_데이터_운영계약_2026-07-28.md`를 따른다. |
| O-15 | **결과 화면은 촬영 당시 프리셋으로 열고, 프리뷰는 노출을 흉내 내지 않는다** | 둘 다 "a"(2026-07-29). **(1) 촬영 세션의 프리셋으로 연다.** `CameraScreen.kt:374`가 `stylePresetId = preset.id`로 촬영 시점 프리셋을 `sessions`에 이미 기록하는데, `ResultScreen.kt:289`는 `settingsRepository.getStylePresetId()` — **온보딩 프로필**을 읽는다. 원래는 의도된 설계였다(TEAM.md §8, "세션 중 카메라 선택은 되돌려 쓰지 않는다") — 프리셋이 *가이드*였을 때는 그 촬영에만 쓰는 값이라 말이 됐다. O-13이 프리셋을 **색감**으로 바꾼 이상 그것은 사진에 붙는 성질이므로, 밤거리로 보며 찍은 사진이 깔끔한 소셜로 열리는 것은 결함이다. O-14의 프리뷰 색감이 들어오면 이 어긋남은 **더 눈에 띈다**. 데이터가 이미 있으므로 읽는 곳만 바꾼다. 기기 사진은 O-12대로 계속 `원본`으로 연다. **(2) 프리뷰는 노출을 빼고 색감만 보여준다.** `FilterEngine.effectiveExposureEv`는 선언된 노출을 **그 사진의 `p99`가 정하는 헤드룸으로 상한**한다(`bright_review`가 +2.18 EV를 선언할 수 있는 이유이자, 밝은 사진이 흰 사각형이 되지 않는 이유). 프리뷰에는 잴 사진이 없다. 공칭값을 그대로 쓰면 밝은 장면에서 프리뷰만 날아가고, 분석 프레임에서 추정하면 통계가 다르고(`frameMean` vs `p99`) 장면이 흔들릴 때 프리뷰 밝기가 함께 떨린다. 따라서 톤커브·화이트밸런스·채도·그레인은 정확히 재현하고 **노출만 뺀다** — 어긋남의 방향이 항상 일정하다(프리뷰가 파일보다 어둡거나 같다). 수용하는 한계: 밝히는 것이 정체성인 프리셋은 프리뷰에서 덜 구분된다 |
| O-14 | **프리뷰 색감은 GL 셰이더로 정확하게 넣는다** | "GL 셰이더로 정확하게"(2026-07-29). O-13 (1)의 구현 방식 확정. 조사 결과 쉬운 길이 없다: 프리뷰가 `SurfaceView`(PERFORMANCE)라 `View.setRenderEffect`가 **닿지 않고**(플랫폼 소스 확인 — `SurfaceView.draw`가 뷰 계층에 구멍을 뚫으므로 View의 RenderNode에는 카메라 픽셀이 없다), Compose 블렌드 오버레이도 같은 이유로 무효다. `RuntimeShader`(AGSL)는 API 33+이고 시연 기기는 31이다. 색상 행렬 근사는 **어두운 영역이 눈에 띄게 틀린다** — `clean_social`이 순검정을 89단계 띄워야 하는데 직선으로는 불가능하고, `bright_review`는 밝은 쪽도 42단계 어긋난다. 따라서 **CameraX `CameraEffect` + `SurfaceProcessor`로 GLES 파이프라인을 직접 쓰고, 톤커브를 LUT 텍스처로 올려 편집기와 정확히 같은 색을 낸다.** ⚠️ **이 기기는 GPU 추론에서 `GL_INVALID_VALUE`로 3/3 실패한 드라이버다**(W3-4). 같은 프로세스에 우리 EGL 컨텍스트를 더하는 것이므로 실패 시 프리뷰가 죽지 않고 **색 없이 계속 도는 폴백이 필수**다. 부수 효과로 O-13이 만든 "카메라에서 프리셋을 탭해도 아무 일도 안 일어난다" 공백도 함께 해소된다 — 별도 결과 화면 변경은 하지 않는다(오너 판단) |
| O-13 | **필터는 색감이고, 구도는 카메라에서만 쓰이며, 가이드는 AI가 고른다** | "나로 해. 다만 구도를 AI가 분석하고 그 구도와 AI 추천 구도 중에 적절한 것으로 가이드를 띄우는 방식으로 해. 즉 구도는 카메라로 촬영할 때만 필요한 거야"(2026-07-29). 세 갈래로 확정된다. **(1) 프리셋은 색감 전용이다** — 프리셋을 고르면 카메라 프리뷰의 **색이 바뀌어야 하고**, 가이드는 건드리지 않는다. 현재는 정확히 반대다: 프리뷰에 색을 입히는 경로가 아예 없고(`ColorFilter`·`RenderEffect` 0건), 프리셋 선택은 `setStyleTarget`으로 **가이드만** 바꾼다. **(2) 레퍼런스는 구도를 가져올 수 있다** — 다만 그것은 가이드를 정하는 **후보 하나**이지 명령이 아니다. AI가 현재 장면을 분석해 낸 추천 구도와 나란히 두고 적절한 쪽을 띄운다(선택 규칙은 리드가 제안하고 오너가 승인한다). **(3) 구도는 카메라 전용 개념이다** — 결과·보정 화면에서 레퍼런스는 색감만 의미가 있다. 이 결정은 §6-2(온보딩 프로필이 스트립 순서를 정함)를 무효화하지 않는다 — 순서는 그대로 두고, 선택이 가이드를 바꾸던 결합만 끊는다 |
| O-12 | **기기 사진에는 자동 보정을 걸지 않는다** | "걸지 않는다"(2026-07-29). W3.5-4 확정. 앱 촬영본은 `conditions_json`이 있으므로 §4-1 자동 보정(수평 회전·노출·화이트밸런스)을 그대로 받지만, MediaStore에서 온 기기 사진은 **열었을 때 원본 그대로** 보인다. 사용자가 고르지 않은 보정이 남의 사진에 적용되지 않게 하는 것이 우선이다. 필터를 직접 고르면 그때부터 적용된다 |
| O-11 | **앨범은 앱 촬영본과 기기 사진을 한 그리드에 섞는다** | "한 그리드에 섞어서"(2026-07-29). W3.5-3 확정. 탭 분리·앱 촬영본 전용을 모두 배제하고 촬영 시간순 단일 그리드로 간다. **리드가 제기한 위험을 오너가 수용한 상태다** — 같은 그리드의 사진인데 탭했을 때 앱 촬영본은 자동 보정이 걸리고 기기 사진은 걸리지 않는다(O-12). 두 종류를 시각적으로 구분할지는 미결이며 W3.5 착수 시 다시 묻는다 |
| O-10 | **AI 2·AI 3 진입점은 필터 스트립 안에 둔다** | "AI2는 카메라와 보정 화면의 아래 필터 선택의 가장 왼쪽에 + 아이콘을 두고 내 필터 만들기로 진입하도록 하자. AI3는 보정에서 내 필터 만들기 바로 오른쪽에 AI로 보정 버튼을 만들어서 할 수 있게 하자"(2026-07-29). **AI 2** — 카메라 화면과 보정(결과) 화면 **양쪽** 하단 필터 스트립의 **가장 왼쪽에 `+` 아이콘**, 탭하면 `내 필터 만들기` 진입. **AI 3** — 보정 화면 스트립에서 `+ 내 필터 만들기` **바로 오른쪽**에 `AI로 보정` 버튼. 따라서 보정 화면 스트립 순서는 `[+] [AI로 보정] [프리셋 6종…] [내 레퍼런스]`이고 카메라 화면은 `[+] [프리셋 6종…] [내 레퍼런스]`다. 이로써 AI 2·AI 3는 **새 화면을 만들지 않고 기존 두 화면에 붙는다**(AI 2 계약서의 "별도 홈 화면은 만들지 않음", AI 3 계약서의 "기존 결과 화면에 표시"와 일치). 별도 진입 버튼·홈 카드·바텀시트 메뉴는 만들지 않는다 |
| O-9 | **EXIF 위치 제거는 서버 수신 직후에 한다** | "서버 쪽에서 지우기"(2026-07-28). O-8이 EXIF 제거를 흐름 차단 조건에서 내린 것은 유지하되, **좌표가 보관·로그에 남는 것은 막는다.** 앱은 사진을 그대로 보내고 `gamdo-server`가 수신 직후 GPS 태그를 제거한 뒤 분석·생성·저장으로 넘긴다. 앱 변경 0. 남는 위험은 전송 구간과 수신 직전이며 이는 수용한다(내부망 시연 전제). 앱의 `ExifSanitizer`는 폐기 해제 대상이 아니다 — 서버가 원천이 되면 앱 쪽 중복 제거는 필요 없다 |

### 승계되는 기존 오너 결정 (유효)

- **디자인 t2(2a·2b·2c·2e·2f)가 화면의 기준이다.** "디자인에 있는 것만 정확하게 구현한다. 없는 것을 만들어내지 않는다" (2026-07-27)
- **D2 유지** — 디자인 2c의 텍스트 안내 카드·점선 원·유도선은 구현하지 않는다 (2026-07-27, P1_Plan_1 §9-7)
- **`+ 필터 저장` 칩 보류** — 착수하지 않는다 (2026-07-27)
- **2026-07-27의 진단 칩·마스크 드래그·사진 살리기 UI 제거 기록**은 역사 기록이다. AI 3 재연결에 필요한 UI는 P1 디자인 계약 후 다시 만든다.
- 커밋은 리드 단독, 기능 단위. 작업은 `main`에서 새 브랜치를 파서 진행 (2026-07-27)

### 리드 판정 (오너 결정에서 파생 — 오너가 이의하면 번복 가능)

| # | 판정 | 파생 근거 |
|---|---|---|
| L-1 | **§6-1의 "조명이 어두워요" 저조도 칩 = 컷** | 디자인 2c에 없는 가시 UI. O-2에서 오너가 가시 비디자인 UI(슬라이더·시트)를 컷하고 비가시 처리만 승인한 패턴을 따른다 |
| L-2 | **D17 로딩 표시는 최소 형태로 신설** | AGENTS.md 제품 결정이 명시 요구(리뷰 #8). "디자인은 모양을 정하지 목록에서 항목을 빼지 않는다"(P1_Plan_1 §9-7의 가이드 토글·비율 토글 선례). 디자인 언어(세이지·소형)로 만든다 |
| L-3 | **AI 2·AI 3 계약 모듈 복구** | 이전 컷 때문에 붙인 `@Deprecated(ERROR)`은 임시 상태다. P2는 레퍼런스 분석·편집 잡·EXIF 전처리·재시도 모듈의 폐기를 해제하고, P1은 새 UI 연결 계약에 맞춰 호출자를 복구한다. 과거 삭제된 화면은 디자인 계약 후 새로 만든다. |
| L-4 | **feedback·consents 테이블은 writer 없이 동결 유지** | AI 2·AI 3을 재개해도 별도 피드백 시트·동의 이력 writer는 우선순위가 아니다. 엔티티·DAO는 스키마 동결 규칙에 따라 남긴다. |

---

## 1. 현재 범위 밖 목록

아래 항목을 구현·복원·개선하는 작업은 전부 범위 밖이다. 발견되는 관련 결함은 수정하지 않고 부록에 기록한다.

1. 레퍼런스 따라 찍기와 사진 살리기는 **활성 범위**다(O-8). P2는 분석·생성·저장 계약을, P1은 화면·내비게이션을 담당한다.
2. AI 3의 결과 비교·마스크 입력·잡 폴링 UI는 P1 디자인 계약이 완료되기 전까지 구현 보류다. 서버·P2 계약은 수정 가능하다.
3. 전후 슬라이더는 AI 3 결과 비교를 위해 재도입 가능하다. 디자인 및 인터랙션은 P1과 별도 계약한다.
4. ~~피드백 시트~~ — §6-3 전체: 1탭 피드백, `feedback` 기록, `ProfileEngine.applyFeedback` 배선, "이 스타일 저장" (O-2)
5. ~~진단 칩 6종~~ — §4-3 (2026-07-27 확정). `captures.problems_json`은 기본값 `"[]"`로 남는 것이 **의도된 상태**다
6. ~~~~D13 레이아웃 버튼+재탐색~~~~ → **재탐색은 구현함** (O-5, 2026-07-28). 레이아웃 **선택** 피커는 여전히 만들지 않는다 — `selectManualLayout()`은 완성돼 있으나 호출자 0으로 남는다
7. ~~저조도 상태 칩~~ — §6-1 일부 (L-1)
8. ~~`+ 필터 저장` 칩~~ (기존 보류 유지)
9. `gamdo-server`의 AI 2 분석·AI 3 생성 경로는 활성 범위다. 인증/다중 사용자/공개 배포 하드닝은 이번 범위 밖이다.
10. ~~공유·다시 찍기~~ (2026-07-27 확정 — 디자인 2f에 없음)

---

## 2. 지켜야 할 사항 (전 에이전트 공통 — 위반은 감사 대상)

### R1. 화면 규칙
- **가시 UI는 디자인 t2(2a·2b·2c·2e·2f)에 있는 것만.** 새 버튼·칩·문구·화면을 만들지 않는다. 단, 이미 오너 승인으로 유지 중인 비디자인 컨트롤(가이드 토글, 4:5/1:1 토글, 저장 실패 문구)과 L-2의 로딩 표시는 예외이며, 예외 목록에 추가하려면 리드→오너 에스컬레이션이 필요하다.
- **비가시 처리(보정 연산·KPI 기록·견고성·성능)는 디자인과 무관하게 진행한다.**
- D2 금지 목록 준수: 행동 지시 문구·방향 화살표·일치도 게이지·자동 촬영 금지. 고정 레이아웃 슬롯의 채움/비움 상태 색상 표시 금지.

### R2. 코드 규칙
- **Room 스키마 동결.** `data/local/entity/**` 수정 금지. 테이블·컬럼 추가/변경 금지. ID 접두사는 DDL v2.0을 따른다(위반 수정은 W2에 명시된 것만).
- **B 모듈 무접촉 원칙.** `guide/AlignmentEngine.kt`, `detect/FrameFeatureCalculator.kt`, `data/ProfileEngine.kt` 등 B 작성 파일은 원칙적으로 수정하지 않는다. 이번 플랜에서 예외로 허용된 수정은 **아래 넷**이며, 수정 시 파일 상단에 `// B 모듈 리드 승인 수정(remain_plan <항목>)` 형식의 주석과 부록 B 통지 기록을 남긴다.

  | 예외 | 파일 | 근거 |
  |---|---|---|
  | W1-7 | `guide/AlignmentEngine.kt` | 플랜 수립 시 승인 |
  | W2-6 | `data/ProfileEngine.kt` | 플랜 수립 시 승인 |
  | O-6 | `detect/EfficientDetSceneDetector.kt` | 2026-07-28 오너 승인 — ML Kit 폴백 제거 |
  | O-7 | `guide/AlignmentEngine.kt` | 2026-07-28 오너 승인 — 브래킷 폭 단위 오류(M1) |
- **public 심볼 삭제 전 트리 전체 grep + 참조 파일 소유자 통지** (TEAM.md 규약 — KDoc 링크는 깨져도 컴파일이 통과한다).
- 새 로직은 가능한 한 **순수 Kotlin(`android.*` import 0)으로 추출**해 JVM 테스트를 붙인다. `android.graphics`·Compose·CameraX 코드는 JVM에서 한 줄도 돌지 않는다.
- 임계값·주기 등 튜닝 값은 하드코딩하지 않고 `guide_config.json`으로 외부화한다(AGENTS §4). 단 `scoring` 네임스페이스는 비워 둔 채 유지한다(기존 리드 판정).

### R3. 검증 규칙
- **빌드·전체 테스트는 spec-test-auditor만 실행.** 구현 에이전트는 `:app:compileDebugKotlin`과 `--tests` 좁은 필터만. `clean`은 웨이브 종료 시 감사자 1회만.
- `JAVA_HOME`은 반드시 gradle 호출과 같은 명령 안에서 JBR(JDK 17)로 지정한다.
- **완료(Done)의 정의는 불변**: "실기기에서 흐름을 끝까지 수행 가능". JVM 통과는 `DONE-JVM`까지만이고 기기 확인 항목은 `DONE-DEVICE` 체크리스트에 적립한다.
- **`📱` 근거는 만료된다.** 해당 경로 코드가 바뀌면 재검증 대상. 기기가 붙어 있는 회차마다 **셔터→저장→갤러리 왕복 30초 스모크**를 반드시 다시 돌린다.
- 더미·고정 이미지로 완료를 위장하지 않는다(AGENTS §7-6).

### R4. 협업 규칙
- **커밋·푸시는 리드 단독.** 에이전트는 제안 커밋 메시지만 제출한다. 웨이브 종료 → 감사 그린 → 리드가 기능 단위로 커밋.
- 작업 브랜치는 `main`에서 새로 판 `remain-p1` 하나로 하고, 웨이브 중간에 `main`으로 병합하지 않는다.
- 공유 파일(`data/AppContainer.kt`, `GamdoApplication.kt`)은 편집 직전 재-Read, 추가만, 알파벳순, 남의 프로퍼티 무접촉. `AndroidManifest.xml`·`GamdoDatabase.kt`는 리드 소유(동결).
- 판정이 필요한 충돌(디자인 vs 규칙, B 모듈 수정 필요 등)은 **작업을 멈추지 말고** 해당 항목만 보류 표시 후 리드에게 에스컬레이션한다.
- `P1_Plan_1.md`·`AGENTS.md`·`docs/**` 수정과 체크박스 `[x]` 전환은 리드 전용.

### R5. 결함 확인 규칙
- 리뷰 보고서의 라인 번호는 `e2bca9c` 기준이다. 현재 HEAD(`a1b557b`)에 object guide 후속 커밋이 들어와 있으므로, **각 결함은 수정 전에 현재 코드에서 재확인**한다. 이미 해소된 결함은 "해소 확인"으로 보고만 하고 건너뛴다.
- 수정마다 그 결함을 재현하는 **회귀 테스트를 먼저** 만들 수 있으면 만든다(순수 함수로 추출 가능한 경우). 불가능하면 사유를 항목에 기록한다.

---

## 3. 팀 구성

`.claude/agents/` 정의를 그대로 쓴다. O-1·O-4로 reference-net의 담당 영역이 대부분 소멸했으므로 **W0 정리 작업 후 해산**한다.

| 에이전트 | 이번 플랜 담당 | 웨이브 |
|---|---|---|
| `guide-capture-agent` 🔵 | 가이드·검출·카메라 결함 소탕, KPI 정직화, 성능 | W1 · W3 |
| `local-edit-agent` 🟠 | §4-1 기하·광학 자동 보정 배선, 결과 화면 결함, 렌더 성능 | W2 |
| `onboarding-polish-agent` 🟢 | §6-2 프로필 배선, 데이터 정합, §7-2 폴리싱 | W2 · W3 |
| `reference-net-agent` 🟣 | W0 도달 불가 코드 처분 + presets 경로 존치 확인 → 해산 | W0 |
| `spec-test-auditor` 🔴 | 각 웨이브 종료 감사·클린 빌드·전체 테스트 (소스 read-only) | 전 웨이브 |

리드 위임 불가 사항: 커밋, 기기 검증 회차, 오너 에스컬레이션, 문서 동기화(§5), B 통지(부록 B).

---

## 4. 웨이브 체크리스트

표기: `[x]` 완료(`📱` = 실기기 근거) / `[~]` 구현됐으나 미완(사유 병기) / `[ ]` 미착수 / ~~취소선~~ 컷.

### W0 — 기반 정리 (reference-net + 리드, 1일 이내)

**목표: 컷 결정을 코드에 반영해 죽은 경로를 제거하고, 검증 환경 결함을 없앤다.**

- [x] W0-1. `gradlew` 실행 비트 커밋 — `git update-index --chmod=+x gradlew` (리뷰: 실행 비트 없이 커밋되어 CI에서 exit 0으로 무동작 성공 처리됨)
  - ✅ 📱 · `100755` 확인 · 커밋 `42fca26`
  - 완료 기준: `git ls-files -s gradlew`가 `100755`
- [x] W0-2. **파괴적 마이그레이션 제거** — [AppContainer.kt:34](app/src/main/java/com/gamdo/app/data/AppContainer.kt#L34)의 `.fallbackToDestructiveMigration()` 삭제 (리뷰 #12. 로컬이 데이터 원천인 앱에서 스키마 불일치 시 14테이블 전부 무음 소실). 버전은 1 유지, Migration 객체는 지금 만들지 않는다
  - ✅ 📱 · 회귀 가드 `DatabaseMigrationPolicyTest`(결함 재주입으로 검증) · 기기: 업데이트 설치 후 14테이블 전부 보존, `user_version=1` 유지 · 커밋 `4d14910`
  - 완료 기준: 삭제 후 기존 설치 위에 업데이트 설치해도 데이터 유지(기기 확인 항목으로 적립). 향후 스키마 변경 시 Migration 없으면 크래시가 나는 것이 **의도된 동작**임을 KDoc에 명시
- [x] W0-3. **A 소유 도달 불가 코드 삭제** (L-3): `ui/result/BeforeAfterSlider.kt`, `ui/result/ResultTabs.kt`와 각 테스트. 삭제 전 트리 grep으로 참조 0 확인(R2)
  - ✅ 📱 · 385줄 삭제, 참조 0 확인 · 커밋 `0c81393`
- [x] W0-4. **B 계약·컷 경로 모듈 `@Deprecated(level = ERROR)` 처리** (L-3): `data/network/GamdoApiClient.kt`의 `createEditJob`/`getEditJob`/`downloadResult`, `data/network/JobTimeoutPolicy.kt`, `data/ReferenceRepository.kt`, `core/ExifSanitizer.kt`, `data/PendingRequestRepository.kt`, `detect/ProblemDiagnoser.kt`. 각 메시지에 "remain_plan §1 컷 확정(O-1/O-2)" 명시. 해당 JVM 테스트는 남긴다(폐기 해제 시 안전망)
  - ✅ 📱 · 8심볼 폐기, 테스트 5파일에 사유 적은 억제. **`ProblemDiagnoser`·`P2ValueDumpTest`는 오너 지시로 보류**(B 통지 대기) · 커밋 `0c81393`
  - 완료 기준: 프로덕션 소스에서 위 심볼 참조 0건, `compileDebugKotlin` 그린
- [x] W0-5. presets 경로 존치 확인 — 컷 이후 앱의 서버 호출이 `GET /presets` 하나뿐임을 grep으로 확정하고, 실패 시 `assets/presets.json` 폴백이 동작함을 테스트로 확인(기존 테스트 있으면 지목만)
  - ✅ · **플랜 전제가 틀렸다** — `getPresets()`도 프로덕션 호출자 0. 컷 이후 앱은 서버 호출을 **하나도 하지 않는다**
- [x] W0-6. (리드) `P1_Plan_1.md` §0.5·§5·§4-3·§6-3과 `P2_Plan_1.md`의 무효 체크박스에 컷·무효 표시 — 특히 `📱` 근거가 `daafdb7` 이전인 항목 전부 (리뷰 §2-1)
  - ✅ · `P1_Plan_1.md` 첫머리에 무효 선언(컷 10개 절 표 + `📱` 전면 무효화) · 커밋 `fb12128`
- [x] W0-7. (감사) 기준선 재확정 — clean 후 `assembleDebug` + 전체 테스트 1회, 결과 수치를 이 문서 밑에 기록. 리뷰 결함 20건 중 현재 HEAD에서 이미 해소된 것 목록화 (R5)
  - ✅ · 기준선 **393 tests / 0 failed**, `assembleDebug` 성공. 리뷰 15건 재확인 결과 잔존 11 · 부분 3 · 해소 1

### W1 — 가이드·검출 결함 소탕 (guide-capture)

**목표: 리뷰 결함 중 카메라·가이드 경로 전부.**

> ⚠️ **이 절은 2026-07-28에 재작성됐다.** 원래 W1-1~W1-10은 리뷰(`e2bca9c` 기준)를 그대로 옮긴 것인데, 현재 HEAD에서 전수 재확인하니 **1건은 이미 해소, 3건은 형태가 변했고**, 그 사이 담당 B가 같은 경로를 크게 바꿨다. 실제 작업 단위(W1-A~)로 다시 적는다. 원래 번호와의 대응은 각 항목에 적었다.

- [x] 📱 **W1-A. 자동 레이아웃이 프리셋 가이드를 덮는 문제 + 점유 머신 삭제** (구 W1-1·W1-2·W1-4 일부 / 리뷰 #1·#8·#10·#20)
  - D2 지시 문구 2종 삭제 + `LayoutGuidePrompt` 타입 제거, 슬롯 점유 색 분기 제거, 모서리 반경 정합
  - `FixedLayoutSlotMatcher`·`SlotMatch`·`SlotMatchStatus`·`allRequiredFilled` 삭제 — 셋 다 프로덕션 호출자를 이미 잃었고 세 버그를 품고 있었다. **그릴 수 없는 표시(D2)의 버그는 고치는 게 아니라 지우는 것**
  - `CameraOverlayD2Test` 신설 — `Text(` 0건·점유 심볼 0건을 소스에서 고정
  - ⚠️ **공존은 최종적으로 채택되지 않았다.** 2026-07-28 상류 병합에서 오너가 상대 방식(게이트 유지 + 재탐색으로 탈출)을 택했다. 커밋 `02f563d` → 병합 `d99b149`에서 게이트 복원
- [x] 📱 **W1-B. 재탐색 버튼** (O-5 / 구 W1-1의 탈출 경로) — 프리뷰 우측 하단, 배율 스톱과 같은 줄. `rescanLayout()`이 `setStyleTarget`보다 좁다(스타일 타깃 보존)는 것을 테스트로 고정. 커밋 `92173fb`
- [x] 📱 **W1-C. 분석 단계별 계측** (구 W1-6의 선행) — `DetectStageTimings`. 실측: 얼굴 98.5 · 포즈 89.8 · 세그 48.2(1회 572.8) · 객체 26.4 · **가이드 체인 0.1ms**. 커밋 `9d1ea72`
- [x] 📱 **W1-D. 사람 신뢰도에서 눈뜸 확률 제거 + 얼굴 분류기 off** (구 W1-3 / 리뷰 #17) — `DETECTED_SUBJECT_FLOOR`를 객체 분기와 공유. 분류기 off의 **성능 근거는 측정으로 반증됐고**(빈 장면엔 분류할 얼굴이 없다) 근거를 "소비처가 결함뿐"으로 좁혔다. 커밋 `5012bc0`
- [x] 📱 **W1-E. 포즈 1/2 스로틀** (오너 결정 / 구 W1-6) — 프레임당 약 29ms 절감(같은 실행 내 귀속). 첫 측정은 발열로 무효였고(**이상치 분포에서 평균은 틀린 통계**) AP 38°C로 식혀 재측정. 커밋 `70865fe`
- [x] 📱 **W1-F. 세그멘테이션 수명** (구 W1-8 / 리뷰 #14·#15) — 타임아웃 제거(오너 결정; `task.cancel()`은 **API에 존재하지 않는다**), null이 캐시를 지우게. 기기: `Image is already closed` 0 · `IllegalStateException` 0. **#15의 null 경로는 기기에서 못 밟았다**(모델이 항상 마스크를 찾음) — DONE-DEVICE 적립. 커밋 `1cd3731`
- [x] 📱 **W1-G. KPI 정직화** (구 W1-10 / 리뷰 #19) — `visible`·`aligned`의 `fixedLayout != null ||` 제거(**둘 다 셔터를 막지 않는데 성공만 보고하고 있었다**), `sgd_`→`gid_`, `resolved` 0→NULL, `aligned`는 래치 중 null. 기기 DB 직접 확인. 커밋 `fe60b9e`
- [x] 📱 **W1-H. 분석 스레드 안전성** (구 W1-9 / 리뷰 #18) — 메인 스레드 변경을 커맨드 큐로 분석 스레드에 위임. **JVM 스트레스 테스트가 수정 전 3/3 재현**(`ConcurrentModificationException`, 그리고 오버레이 `RectN(NaN,NaN,NaN,NaN)`), 수정 후 5/5 통과
- [x] 📱 **W1-I. 전면 카메라 가이드 미러** (구 W1-5 / 리뷰 #9) — `OverlayMapping` 신설로 분석공간(미러함)과 구도공간(미러 안 함)을 분리. 결함 재주입 시 가드 3건 실패 확인. 기기 SM-G970N: `third_left`(밤거리) 프리셋이 후면·전면 **양쪽 모두 왼쪽 1/3**에 브래킷을 그림(후면 중심 360/1080, 전면 브래킷 우측 팔 479)
- [x] 📱 **W1-J. 브래킷 폭 25% 축소** (구 W1-7 / 리뷰 M1) — O-7로 승인되어 `CompositionFrame`으로 계산을 뽑고 `AlignmentEngine.targetFrame`·`SceneLayoutGuide.targetFrame` 양쪽이 공유하게 했다(두 곳이 갈라지지 못하게). 커밋 `6b58b82`
- [x] 📱 **W1-K. matchScore의 15%가 상수** (리뷰 M2) — `ShutterFrame.observedHorizonPosition` 신설 후 두 호출부에 배선. **`MatchScoreCalculator.kt`(B 인접 파일)는 무접촉** — 파라미터를 이미 받고 있어 호출부만 고치면 됐다. 미검출 시 타깃 값으로 폴백해 감점하지 않는다(`SceneProposalEngine`의 기존 관례와 동일). 결함 재주입으로 가드 검증. 기기: 촬영·`final_match_score` 기록 정상 — 다만 빈 벽이라 수평선 미검출이어서 **이 수정의 효과는 기기에서 분리 관측되지 않았다**
- ~~구 W1-3의 `choose()`/`match()` 자격 모순 (리뷰 #16)~~ — **이미 해소**. 재확인 결과 상류가 양쪽을 같은 술어로 통일했고 반박 에이전트도 확인
- ~~구 W1-6의 마스크 필수·레이블 화이트리스트 (리뷰 #11 후반)~~ — **이미 해소**. 상류가 `isValidBox`/`isSemanticMatch`로 분리
- ~~구 W1-4의 D17 로딩 표시 (L-2)~~ — **미착수**. 아래 부록 C 참조

**성능 현황 (D15 재정의 대상, 오너 승인):** 객체 분석 실효 **1.28 → 5.52 FPS**. 목표 8 미달이며 냉각 상태 수치다. 25초 촬영에 AP가 38.0→54.6°C로 오르므로 **냉각 수치는 실사용에서 도달 불가능한 최선값**이다. ⚠️ 상류가 객체 검출기를 EfficientDet로 교체(`8c92fff`)해서 단계별 실측은 **만료** — 재측정 필요.

### W2 — 보정 파이프라인·데이터 정합 (local-edit + onboarding-polish, 병렬)

**목표: 오너가 승인한 유일한 비디자인 기능(기하·광학 자동 보정)을 배선하고, 온보딩 개인화를 실제로 동작시킨다.**

local-edit:
- [x] W2-1. **기하·광학 자동 보정 배선** (O-2, 리뷰 #6) — 결과 화면이 사진을 열 때: `conditions_json`의 `tiltDeg`로 수평 회전 → 비율 크롭(`subject` 있으면 인물 중심 유지) → 자동 노출(±1EV 내)·화이트밸런스 → 그 위에 기존 `FilterEngine` 색 패스. **화면 UI 불변**(R1) — 다이얼·눈금자가 읽고 쓰는 값 체계는 지금과 동일하게 유지
  - 엔진 경계 확정: **기하·광학 = `LocalEditor` 계열, 색 = `FilterEngine`.** `LocalEditor.kt` 헤더의 "한 번에 하나만 연결" 서술을 이 경계로 갱신(리뷰 §3-4의 미결 해소)
  - 완료 기준: JVM — 8° 기울인 `conditions_json` 입력에서 보정 계획이 -8° 회전을 포함. 기기 적립: 기울여 찍은 사진이 결과 화면에서 수평으로 열림 + **2초 이내**(§4-1 예산)
- [x] W2-2. **저장 정직성** (리뷰 §3-4) — 풀 해상도 디코드 실패 시 1440px 프리뷰를 저장하면서 "갤러리에 저장됨"으로 보고하는 경로 수정: 실제 저장물 기준으로 보고하거나 실패를 `saveError`로 노출
- [x] W2-3. **눈금자 렌더 처닝** (리뷰 §3-4, §4-2 60fps) — 눈금 틱마다 13MB 비트맵 재할당·미회수 수정(비트맵 재사용 또는 디바운스+저해상 프리뷰). 수치 근거를 항목에 기록
  - 완료 기준: 기기 적립 — 드래그 중 프레임 드랍 체감 없음, 메모리 프로파일 1회 첨부

onboarding-polish:
- [x] W2-4. **`ProfileEngine.recommend()` 배선** (§6-2 미배선 해소) — 온보딩 카드 선택 → 프로필 → 추천 상위 3종이 카메라 스타일 스트립 순서를 실제로 결정. 현재 하드코딩 맵 제거
  - 완료 기준: JVM — 서로 다른 카드 2세트가 서로 다른 상위 순서 산출. 기기 적립: 온보딩 리셋 후 재선택으로 스트립 순서 변화 확인
- [x] W2-5. **`CardRepository` 일원화** (리뷰 §3-4) — `OnboardingScreen`의 `cards.json` 인라인 재파싱을 `CardRepository`로 통일(죽은 쪽을 살리고 중복을 제거 — 어느 쪽을 정본으로 할지는 테스트가 많은 쪽)
- [x] W2-6. **켈빈 정규화 버그** (TEAM.md B 미결 #1) — `ProfileEngine.recommend()`의 색온도 차원이 정규화 없이 순위를 지배하는 버그. W2-4가 이 버그 위에 배선되면 추천이 무의미하므로 **B 모듈 리드 승인 수정**으로 처리(R2 절차 준수, 기존 카나리아 테스트 `PresetProfileMapperTest` 활용)
- [x] W2-7. **2b 요약 3줄 동적화** — 하드코딩 요약을 프로필 파생 값으로(§6-2 잔여, `ProfilePalette` 선례). 어떤 카드를 골라도 동일한 현재 상태는 AGENTS §7-6(더미 위장) 판정 대상
- [x] W2-8. **`card_selections` ID 정합** — `"round-1:$cardId"` 조합 문자열 → `sel_` + ULID (DDL 준수, [ProfileRepository.kt:22](app/src/main/java/com/gamdo/app/data/ProfileRepository.kt#L22)). 중복 방지 로직이 ID 형식에 의존하고 있었다면 별도 컬럼/쿼리로 대체
- 웨이브 완료 기준: 감사 그린. **파일 교차 0** — local-edit은 `edit/`·`ui/result/`, onboarding-polish는 `data/Card*`·`data/Profile*`·`ui/onboarding/`만. 교차 발생 시 경계 설계 오류로 즉시 리드 보고

### W3 — 성능·하드닝·폴리싱 (guide-capture + onboarding-polish)

**목표: §6-1(재정의)·§7-1·§7-2. 시연 중 무너지지 않게 한다.**

- [ ] W3-1. **분석 주기 외부화·발열 대응** (§7-1) — `FrameAnalyzer` fps 리터럴 `12` → `guide_config.json`. 발열 시 자동 하향(8fps) 규칙 구현. 값 이중화 금지(에셋이 단일 진실)
- [x] W3-2. **프리뷰 FPS 계측 분리** (§7-1) — 커밋 `bdba363`(계측 신설)·`7a9177c`(디버그 게이트). **이미 충족돼 있었다**(감사 2026-07-30). `PreviewFrameMeter`(순수 코틀린, JVM 7케이스) + `PreviewView.setFrameUpdateListener` 기반 `attachPreviewFrameProbe`. HUD가 `분석 …fps · drop …%`와 `프리뷰 …fps`를 별도 줄로 표기하고, 계측 불가 시 0 대신 사유를 표기한다(`PreviewFpsAvailability` 4상태). 같은 작업에서 `AnalysisStats.fps`의 나눗수 버그(4.2fps를 6fps로 부풀리던 것)도 해소됐다
  - ⚠️ 계측은 debug 전용이고, 켜는 대가로 `PreviewView`가 `COMPATIBLE`(TextureView)로 내려간다. **시연 기기가 읽는 fps는 릴리스 경로의 하한이다** — §7-1 판정 방향으로는 안전하나 debug와 release를 비교하면 debug가 느린 것이 정상이다
  - 기기 확인(2026-07-30): `프리뷰 24fps` → 담당 B의 세그멘테이션 생략·객체 추론 정지 이후 **29fps**. §7-1의 30FPS가 사정권
  - [ ] 📱 10분 연속 촬영 후 프리뷰 fps 기록 — 기기 적립 (계측 수단은 준비 완료)
- [ ] W3-3. **§6-1 하드닝(재정의 범위)** — 네트워크 단절 시 로컬 기능 전부 유지(생성·레퍼런스는 컷됐으므로 확인 범위는 presets 폴백뿐), 인물 미검출 시 인물 오버레이만 숨김(수평선·프레임 유지, 문구 추가 금지), 저장 공간 부족·카메라 점유 충돌·백그라운드 복귀 재바인딩
  - 완료 기준: 기기 적립 — 각 상황을 인위 재현하며 크래시 0건 (~~저조도 칩~~ L-1, ~~생성 타임아웃~~ O-1은 확인 대상에서 제외)
- [ ] W3-4. **콜드 스타트** (§7-1) — 앱 실행→첫 분석 2초 이내 실측, 불필요 로그 제거
  - **⚠️ `preferGpu=false`를 전역으로 넣지 마라.** SM-G970N에서 콜드 스타트 7.9초의 99.3%는 EfficientDet TFLite 로드이고, 그 빌드는 **버려진다**: GPU 델리게이트 *생성*은 성공하고(`accelerator=GPU degraded=false`) 첫 *추론*에서 `GL_INVALID_VALUE … glMapBufferRange`로 실패한 뒤 CPU로 111ms 만에 재빌드된다(3회 연속 재현, 2026-07-29). 이 기기만 보면 GPU를 끄는 것이 7.6초를 없애지만, **담당 B 환경에서는 GPU가 정상 동작한다(오너 확인, 2026-07-29)**. 따라서 전역 플래그는 답이 아니다. **다만 해법을 정하기 전에 교란 변수를 먼저 제거해야 한다.**

  - **교란 변수 해소됨(2026-07-29 측정).** 앞의 두 숫자는 같은 조건이 아니었다 — 7570ms는 콜드 프로세스의 첫 모델 읽기를 포함하고 111ms는 같은 프로세스의 재빌드였다. 그래서 격리된 worktree에서 `DetectorAcceleratorReport.plan`을 CPU 전용으로 강제해 **콜드 프로세스 CPU 빌드**를 2회 쟀다:

    | | object | total |
    |---|---|---|
    | GPU 시도(현행) | 7570 · 7933ms | 7630 · 7992ms |
    | **CPU 전용(콜드)** | **281 · 228ms** | **376 · 313ms** |

    **7.5초는 모델 파일 읽기가 아니라 GPU 델리게이트 컴파일이다.** 모델을 처음 읽는 CPU 빌드도 250ms 남짓이다.
  - **따라서 해법은 CPU 선행 + GPU 후행 검증이다.** CPU로 먼저 만들어 즉시 가이드를 띄우고(≈350ms), GPU는 백그라운드에서 만들어 **실제 추론 1회까지 성공**한 경우에만 교체한다. 이 설계는 (a) 기기별 기록이 아예 필요 없어 "한 번 실패하면 영원히 GPU 못 씀" 문제가 발생하지 않고(오너 지적, 2026-07-29), (b) GPU가 정상인 담당 B 환경에서도 콜드 스타트가 7.6초 빨라지며, (c) 실패가 첫 추론에서만 드러나는 이 기기의 증상을 정확히 반영한다(생성은 성공하고 추론이 실패한다). 콜드 스타트 목표 2초(§7-1)는 이 변경으로 비로소 사정권에 들어온다.
  - **기기별 기록은 이제 불필요하지만, 혹시 도입하더라도 불린은 금지.** 실패 1회를 영구 차단으로 기록하면 일시적 실패(GPU 메모리 압박, 드라이버 일시 오류, 다른 앱의 GL 점유)로도 그 기기는 영원히 CPU가 되고, GPU가 멀쩡한 담당 B 환경에서도 한 번 어긋나면 조용히 CPU로 내려앉는다(오너 지적, 2026-07-29). 필요하다면 **서명 기반**으로 — 앱 버전 + 모델 파일 + OS 빌드 + GPU 드라이버 버전을 묶어 기록하고 하나라도 바뀌면 재시도, 여기에 "연속 N회 실패" 조건을 더한다.
  - 착수 전 오너 승인 필요(담당 B 모듈·저장소 동시 수정)
- [~] W3-5. **§7-2 폴리싱** — 감사 결과(2026-07-29) 대부분이 **이미 충족돼 있거나 불필요**했다. 남은 것은 폰트 하나이고 시연 후로 미뤘다
  - ~~**아이콘**~~ — 이미 있다. `drawable/ic_launcher_foreground.xml` + `mipmap-anydpi-v26/`에 세이지 링·차콜 디스크의 조리개 마크 어댑티브 아이콘이 `2456158`부터 들어 있다. **리드가 밀도별 mipmap이 없는 것만 보고 템플릿이라 단정했던 것은 오판이다**(D17 로딩 표시 때와 같은 실수 — 있는 것을 다시 만들 뻔했다)
  - ~~**스플래시**~~ — 시연 기기(API 31)에는 불필요하다. `Theme.Gamdo`의 `windowBackground`가 이미 `charcoal_900`이고 API 31+는 그 배경과 런처 아이콘으로 시스템 스플래시를 자동 표시한다. `androidx.core:core-splashscreen`은 API 30 이하 백포팅용이므로 minSdk 29를 지키려면 그때 넣는다(의존성·매니페스트 패치는 감사 보고서에 기록됨)
  - ~~**다크 테마 통일**~~ — `ui/theme/`는 정합하다. D11-4의 네 색값이 정확히 존재하고 `lightColorScheme`·`isSystemInDarkTheme`·`DayNight` 사용처가 0건이다(D10-1 준수). 다만 토큰을 안 쓴 하드코딩 4곳이 남아 있다: `OnboardingScreen.kt:357`(회색 리터럴 → `OnDarkMedium` 후보), `CameraOverlay.kt:32`(`HorizonRed` 지역 선언 → D11-5상 공용 토큰 요청 대상), `CameraScreen.kt:1485,1530`·`ReferenceStrip.kt:205`(`0x99141614` — `Charcoal900`과 한 스텝 차이, `Charcoal900.copy(alpha=0.6f)` 의도로 보임). 전부 타 소유 파일이라 보고만 한다
  - ~~**문구 검수**~~ — 전문 용어 0건 확인. `IoU`·`matchScore` 등은 `BuildConfig.DEBUG` 게이트 안의 HUD에만 있다. `대비`·`채도`는 디자인 2f가 지정한 소비자 사진 편집 용어이지 엔지니어링 용어 누수가 아니므로 유지
  - **HUD 릴리스 게이트** — 플랜은 "릴리스에서 개발자 제스처로만"이지만 현재는 `BuildConfig.DEBUG`로 **릴리스에서 아예 컴파일 제외**된다. 요구보다 엄격하며 사용자에게 노출될 경로가 없으므로 **의도는 충족된 것으로 판정한다.** 제스처 해제가 필요하면 지속되는 `developerModeUnlocked` 플래그가 필요하고 이는 별도 착수 대상
  - [ ] **Pretendard 폰트 번들** — **시연 후로 연기(오너 결정 2026-07-29).** 레포지토리에 폰트 바이너리가 없고 Google Fonts 카탈로그에도 없어 다운로드형 폰트도 불가하다. 지금은 시스템 폰트로 간다 — 타이포그래피 차이는 보이지만 시연을 막지 않는다
- [ ] W3-6. (리드 판정 필요 시) 기동 후 첫 프리뷰 제스처 유실 — 기존 "별건" 판정 유지. `surfaceProvider` 바인딩 전환은 이번 범위에서 착수하지 않되, W3 중 기기 회차에서 비용(앱 실행당 탭 1회)이 시연에 문제되는지 리드가 재판정
- [x] 📱 **W3-7. 셔터→저장 지연** (오너 보고 2026-07-29: "셔터 입력후 사진 저장까지 너무 느려") — 계측을 먼저 붙였다. logcat만으로는 구간을 나눌 수 없기 때문이다: `TakePictureManager`의 `No new request`는 CameraX가 *요청*을 끝낸 시점이지 우리 일이 끝난 시점이 아니고, 전체 해상도 JPEG 디코드와 비트맵 복사가 **CameraX 자기 콜백 안에서** 돈다. `CapturePhase.CAMERA_X`를 `onCaptureSuccess`의 첫 문장으로 못박아 둘을 갈랐다. 커밋 `eda9306`, `6b080ff`

  | 구간 | 착수 전 | 완료 후 |
  |---|---|---|
  | decode | 844~953 | **745~833** |
  | crop | 38~60 | **2~13** |
  | encode | 229~290 | 227~264 |
  | 갤러리 내보내기 | 대기에 포함 | **대기에서 제외**(79~180ms) |
  | **앱 구간 합계** | **~1215ms** | **~1019ms** |

  - **`total=`로 비교하면 안 된다.** `cameraX`가 한 설정 안에서 290~1613ms로 흔들린다 — 책상 위 평평하고 어두운 장면(luma 0.40)이라 3A가 수렴을 못 한다. **동일 빌드·동일 냉각 시작에서 중앙값이 1678ms와 2687ms로 갈렸다.** 세션 간 편차가 재려는 효과보다 크므로 3A에 영향받지 않는 앱 구간만 비교한다. 리허설 수치(셔터 5.4초·열기 10.7초)가 리드 자신의 `sleep`이었던 것과 같은 종류의 오류다
  - **분석 일시정지는 효과가 없었다.** 같은 냉각 조건 5샷씩 앱 구간 1213ms(off) 대 1203ms(on), 10ms 차이. 디코드가 이미 `Dispatchers.Default`로 8코어에 퍼져 있어 스레드 하나를 돌려받아야 이론상 12%다. **오너 판단으로 유지**하되(저사양 기기 가설) KDoc에 "결과가 아니라 가설"이라고 적었다. 해제는 3중(셔터 `finally`·화면 `onDispose`·4초 워치독), 기기에서 촬영 도중 앨범 왕복 후 `watchdogTrips=0`
  - **비트맵 복사 4회 → 1회.** `cropped→rotated→mirrored→centerCropToRatio` 체인이 12MP에서 45MB짜리 중간 산물을 3~4개 만들고 마지막만 남겼다. 네 단계를 정수 연산으로 합성해(`CaptureGeometry`, `android.*` 임포트 0) `createBitmap` 한 번에 적용한다. **기기 검증: 구도가 정확히 같다** — 치수 3024×3780 동일, 0픽셀 이동에서 diff 최소(±1은 더 나쁨), 전면 저장본이 프리뷰와 같은 방향(상관 +0.23 대 거울 −0.10)
  - `saved_to_gallery`를 `Boolean`→`GalleryExport` 4상태로. 내보내기가 반환 뒤로 밀리면서 `false`가 "거부됨"과 "아직 안 함"을 동시에 뜻하게 되는데, **한 플래그가 두 뜻을 갖는 것이 W2-2가 일어난 방식이다.** 컬럼은 상태에서 파생시켜 호출부가 둘을 어긋나게 쓸 수 없게 했다. 화면 문구는 추가하지 않았다 — 없는 주장은 틀릴 수 없다
  - **남은 지렛대는 JPEG 디코드 자체(~600ms)다.** 복사 1회가 60~90ms이므로 지금 `decode` 구간의 대부분은 12MP JPEG 디코딩이다. 미해결: 프로세스 종료 시 갤러리 내보내기 재시도 없음, 셔터 직후 앨범 이동 시 촬영 취소(이 변경 이전부터의 동작)
- 웨이브 완료 기준: 감사 그린 + clean 빌드 1회 + 기기 확인 적립 소진 시작

### W3.5 — 앨범 전체 사진 접근 (오너 결정 2026-07-28, 신규)

**목표: 앨범이 기기 사진 라이브러리를 직접 읽는다. 한 장씩 가져오는 방식을 대체한다.**

오너 결정: *"추후에 앨범의 전체사진권한을 얻도록 할거여서 가져오기 버튼은 필요없음."* `가져오기` 칩은 제거했고(디자인 2e에 없었고 §4-3 컷으로 명분도 사라졌다), 그 자리를 이 항목이 대신한다.

- [ ] **W3.5-1. 권한 모델 확정** — 이 앱은 **minSdk 29**(이 문서가 26이라 적었던 것은 오류 — `app/build.gradle.kts:29` 확인, 2026-07-29) / targetSdk 35이므로 API 레벨마다 다른 권한이 필요하다. `core/AppPermissions.kt`가 이미 분기를 갖고 있으니 그 위에 얹는다.

  | Android | 권한 | 성격 |
  |---|---|---|
  | 12 이하 (API ≤32) | `READ_EXTERNAL_STORAGE` | 전체 접근 |
  | 13 (API 33) | `READ_MEDIA_IMAGES` | 전체 접근 |
  | 14+ (API 34+) | `READ_MEDIA_IMAGES` + `READ_MEDIA_VISUAL_USER_SELECTED` | **부분 접근이 기본** — 사용자가 고른 사진만 보일 수 있다 |

  **부분 접근이 이 기능의 핵심 함정이다.** API 34+에서 사용자가 "일부만 허용"을 고르면 라이브러리 전체가 아니라 선택분만 읽힌다. 그 상태를 "권한 거부"로 취급하면 안 되고, "전부 보여주는 중"인 척해도 안 된다. 어느 쪽인지 판정하는 로직을 **순수 함수로 뽑아** JVM 테스트를 붙인다(`PhotoAccessLevel.of(sdkInt, granted)` → `FULL` / `PARTIAL` / `NONE`).

- [x] 📱 **W3.5-2. MediaStore 질의** — 구현·실기기 확인 완료(2026-07-29). 앱 촬영본과 기기 사진을 촬영 시간순 한 그리드로 합치고 `MediaStore.Images`를 페이지 단위로 읽는다. **중복 제거**: `CaptureRepository.exportToGallery`가 내보낸 사진을 `Pictures/감도`에 쓰므로 같은 JPEG가 `captures` 행과 MediaStore 행으로 두 번 존재한다. `GALLERY_BUCKET_NAME`(공용 상수로 승격)으로 MediaStore 쪽을 걸러 `captures` 행만 남긴다 — captureId를 가진 쪽이라야 탭했을 때 열린다.
  - **실기기 대조 검증**: `captures` 4행 · `감도` 버킷 50행 · 전체 라이브러리 453장. 상위 9칸을 최신 앱 촬영본과 픽셀 비교해 **정확히 1칸만 일치**(12.9 vs 나머지 56~114). 정렬도 앱 촬영본(12:26:00)이 카메라 사진(12:25:26)보다 앞 — 밀리초/초 단위 혼동이 있었다면 여기서 어긋났다
  - **⚠️ 고아 내보내기가 이미 46건 존재한다.** 버킷 통째 필터라 `captures` 행이 없는 내보내기는 앨범에서 보이지 않는다. 착수 시에는 "재설치·데이터 삭제가 있어야 발생"으로 봤으나, 개발 중 반복 재설치로 **이 기기에서 이미 50건 중 46건이 그 상태다.** 수용 근거를 "발생하기 어렵다"가 아니라 **"이미 발생했으나 시연은 새로 찍은 사진으로 하므로 무해하다"**로 정정한다. `deleted_at`에 쓰기가 생기는 순간 재검토 대상
- [ ] ~~**W3.5-2 잔여**~~ → 아래 W3.5-6으로 분리
- [ ] **W3.5-6. 기기 사진 탭 → 보정 화면** (신규 분리) — `ui/result/**`를 AI 3가 쓰고 있어 이번 패스에서 뺐다. `Routes.RESULT`는 `captureId`를 받고 `ResultScreen`은 `File(filePath)`로 읽으므로, MediaStore `Uri`는 `ContentResolver.openInputStream`이 필요하다. 또한 현재 `capture != null`이면 기하·광학 보정이 무조건 걸리므로 **O-12를 지키려면 출처 분기가 필요하다.** ⚠️ **리드 판단 필요** — 에이전트가 제시한 A안(`importGalleryPhoto`로 `captures` 행을 만들어 기존 경로 재사용)은 **같은 사진이 앱 촬영본과 기기 사진으로 두 번 나오게 되므로 채택하지 않는 쪽으로 기운다.** B안(Uri 네이티브 경로)이 중복을 만들지 않으나 파일을 더 건드린다
- [ ] ~~**W3.5-2 원문**~~ — `MediaStore.Images` 커서를 페이지 단위로 읽어 앨범 그리드에 붙인다. 현재 그리드는 `capturesDao().getRecent(60)` 한 번으로 끝나므로, 수천 장을 한 번에 읽지 않도록 페이징이 필요하다. 커서 조회·정렬·페이지 경계 계산은 `android.*` 없이 뽑아 테스트한다.

- [ ] **W3.5-3. 앱 촬영본과 기기 사진의 관계 결정** — ⚠️ **오너 판단 필요.** 앨범에 (a) 기기 사진만, (b) 앱 촬영본만, (c) 둘을 한 그리드에 섞어서, (d) 탭으로 분리 중 무엇을 보여줄지. 앱 촬영본은 `captures` 행과 `conditions_json`을 갖고 있어 §4-1 자동 보정이 걸리지만 기기 사진은 그것이 없다 — **같은 그리드에 섞으면 탭했을 때 동작이 달라진다.** 결정 없이 착수하지 않는다.

- [ ] **W3.5-4. 기기 사진을 열었을 때의 보정 경로** — `conditions_json`이 없으므로 기울기를 모른다. `CaptureConditions.NONE`으로 0도 보정이 되는 것은 이미 동작하지만(§4-1 배선 시 확인), **광학 보정(자동 노출)은 여전히 걸린다.** 사용자가 고르지 않은 보정이 남의 사진에 적용되는 것이 맞는지 판단이 필요하다.

- [ ] **W3.5-5. `가져오기` 잔재 처분** — ~~`CaptureRepository.importGalleryPhoto`는 이제 프로덕션 호출자가 0이다. 이 기능이 그것을 대체하므로 L-3에 따라 삭제하거나 폐기 표시한다.~~ **정정(2026-07-30): `importFromGallery`는 유지한다 — 위임 수신 사진이 그것을 부른다.** QR `나 찍어줘`로 받은 사진을 `captures` 행으로 만드는 `importReceivedShootPhoto`가 `importFromGallery(uri, source = DELEGATED_SHOOT)`로 위임하므로, 이 메서드에 **첫 프로덕션 호출자가 생겼다.** 사설 복사·디코드 후 재인코드로 EXIF/GPS 탈락(D8)·회전 baking·`insertCapture`를 한 곳에서 하는 경로이므로 복제하지 않고 재사용하는 것이 맞다. 지우면 D8 위치정보 제거 규칙이 두 곳으로 갈라진다. 처분 대상으로 남는 것은 **`importGalleryPhoto`(얇은 별칭)와 그 UI 진입점뿐**이고, `importFromGallery` 본체는 아니다.

- [ ] 📱 **완료 기준**: 실기기에서 권한 부여 → 기기 사진이 그리드에 보임 → 한 장 탭 → 보정 화면 정상 → 저장. API 34+ 기기가 없으므로 **부분 접근 경로는 이번 범위에서 실기기 확인 불가**이며, 그 사실을 부록 C에 기록한다.

---

### W4 — 시연 준비 (리드 전담)

- [ ] W4-1. 시연 모드(§7-3 축소) — 숨김 설정: 온보딩 리셋, 데모용 스타일 고정. **디버그 빌드/개발자 제스처 뒤에만** 존재(R1)
- [ ] W4-2. 단일 시연 흐름(온보딩→가이드 촬영→보정→저장) **3회 연속 성공**까지 리허설, 실패 시 `guide_config.json` 현장 조정
- [ ] W4-3. 성공 시연 전 과정 화면 녹화 확보
- [x] 📱 **W4-4. APK 백업 2부** — `C:\Madcamp\gamdo-apk\gamdo-demo-b0c4296.apk`와 기기 `/sdcard/Download/`에 각 1부. 파일명에 커밋 해시를 박아 어느 코드로 만든 APK인지 추적된다. 저장소 밖에 둔다(166MB)
  - **시연은 `demo` 빌드로 한다** (`./gradlew :app:installDemo`). `debug`로 시연하면 보정 필터 탭마다 1초가 보인다 — ART가 `debuggable` 앱의 최적화 JIT을 제한하고 필터가 스칼라 픽셀 루프라 그대로 맞는다(1059ms 대 55ms, 같은 코드·같은 기기·같은 사진). `edit/ParallelFilter.kt` KDoc에 수치와 재현법
  - 부수 효과로 HUD가 컴파일에서 빠져 카메라 화면이 깨끗해진다(`BuildConfig.DEBUG == false`). P1-C1/P1-4가 요구하는 모습이 이미 나온다
  - APK 246MB → 166MB: 쓰이지 않는 x86·x86_64 네이티브 라이브러리 84MB 제거(`abiFilters`). `armeabi-v7a`는 예비 기기가 미정이라 남겼다
- [ ] W4-5. 컷 목록(§1) 발표용 정리 — "다음 단계"로 언급할 것들

### 웨이브 공통 — 기기 검증 회차 (리드, 기기 연결 시마다)

- [ ] 매 회차: 셔터→저장→갤러리 왕복 30초 스모크 (R3)
- [ ] W1 적립분: 프리셋 가이드 1분 생존 · 전면 렌즈 구도 일치 · KPI 3행 정직 기록
- [ ] W2 적립분: 기울인 사진 수평 복원 2초 이내 · 눈금자 드래그 무드랍 · 카드 2세트 스트립 순서 상이
- [ ] W3 적립분: §6-1 상황별 크래시 0건 · 10분 발열 · 콜드 스타트 2초

---

## 5. 문서 동기화 의무 (리드)

1. W0-6의 기존 플랜 무효 표시는 W0 종료 전에 끝낸다 — 리뷰가 지적한 "가장 부정확한 절"(P1_Plan_1 §0.5)을 방치한 채 새 작업을 얹지 않는다.
2. 각 웨이브 종료 시 이 문서의 체크박스를 갱신한다. `[x]` 전환은 리드만, `📱`는 기기 근거가 있을 때만.
3. 오너 결정·리드 판정이 추가되면 §0에 날짜와 함께 기록한다. §0에 없는 결정은 존재하지 않는 것으로 간주한다.

---

## 부록 A. 컷으로 죽는 코드의 최종 상태 (W0-3·W0-4 결과 표)

| 코드 | 처분 | 근거 |
|---|---|---|
| `ui/result/BeforeAfterSlider.kt` + 테스트 | 삭제 | A 소유, O-2 컷 |
| `ui/result/ResultTabs.kt` | 삭제 | A 소유, 디자인 2f에 탭 없음 |
| `data/network/GamdoApiClient.kt` edit-job 3메서드 | `@Deprecated(ERROR)` | B 계약, O-1 컷 |
| `data/network/JobTimeoutPolicy.kt` | `@Deprecated(ERROR)` | 〃 |
| `data/ReferenceRepository.kt` · `core/ExifSanitizer.kt` | `@Deprecated(ERROR)` | O-1 컷. 업로드 자체가 없어져 D8-5 가드 미실행은 위험이 아니라 비적용이 됨 |
| `data/PendingRequestRepository.kt` | `@Deprecated(ERROR)` | 잔여 서버 호출이 presets 폴백뿐이라 재시도 큐 소비처 소멸 |
| `detect/ProblemDiagnoser.kt` · `edit/computeImageMetrics` 진단 경로 | `@Deprecated(ERROR)` | §4-3 컷 확정 |
| `detect/ImageMetricsExtractor.kt` | `@Deprecated(ERROR)` 유지 | 기존 처분 |
| `feedback`·`consents` 엔티티·DAO | 동결 유지(writer 없음) | L-4 |

## 부록 B. 담당 B 통지 목록 (리드가 전달)

1. **B 모듈 수정 2건** — `AlignmentEngine.kt` 비율 이중 적용(W1-7), `ProfileEngine.recommend()` 켈빈 정규화(W2-6). 수정 diff와 회귀 테스트 링크 첨부
2. 시나리오 A·B 컷 확정(O-1) — `/references/analyze`·`/edit-jobs`의 앱 쪽 호출자가 영구 0이 됨. B의 지표 스크립트 중 `edit_results_local` 의존분은 데이터가 없음
3. 서버 결함은 이번 플랜 범위 밖(O-4) — 부록 C 참조
4. `blurStrength` 의도적 미적용, `scoring` 네임스페이스 공백 유지 — 기존 통지 재확인

## 부록 C. 알려진 위험 (이번 범위 밖 — 수정하지 않음)

- **서버**: edit-job 조회 IDOR(`edit_jobs.py:333`) · 워커 `UnboundLocalError` 영구 종료(`comfyui_provider.py:166`) · 클린 체크아웃 기동 실패(`main.py:24`) · 보존 기간 3종(`worker.py:124` 외) · `/references/analyze` 무제한 업로드 — 전부 `review_report.md` §1 기준. 서버를 실기동할 일이 생기면 이 목록을 먼저 볼 것
- ~~**D17 로딩 표시 미구현** (L-2)~~ → **이미 구현돼 있다.** [CameraOverlay.kt:216](app/src/main/java/com/gamdo/app/ui/camera/CameraOverlay.kt#L216)이 `layoutState is GuideLayoutState.Searching`일 때 `CircularProgressIndicator`를 그린다. 담당 B가 `3e251dd`에서 만든 것을 리드가 `d99b149`로 병합해 놓고 이 기록을 갱신하지 않았다 — **감사(2026-07-28)가 잡았다.** W3-5에서 있는 것을 다시 만들 뻔했다
- **앱(수용된 위반·갭)**: **D13 수동 레이아웃 선택 미제공** — 2026-07-28 상류 병합에서 담당 B가 만든 상단 `구도` 드롭다운(재탐색 + 레이아웃 목록)을 제거하고 재탐색만 프리뷰 버튼으로 일원화했다(오너 결정). `CameraViewModel.selectManualLayout`과 `availableManualLayouts`는 완성된 채 호출자 0으로 남아 있으므로, 되살릴 때는 UI만 붙이면 된다 · 기동 후 첫 프리뷰 제스처 유실(W3-6에서 재판정) · `captures.problems_json` 항상 `"[]"`(§1-5 컷 귀결) · FK 미선언·인덱스 DESC 미지정(스키마 동결 유지 — 시연 후 마이그레이션 과제로 이월) · §7-1 안내 갱신 250ms vs 목표 200ms(`alignedEnterFrames`×fps 산술 — W3-1 외부화 후 값 튜닝으로 해소 시도)

## 부록 D. 시연 후 과제 (이 플랜이 끝나도 남는 것)

1. FK 8개·인덱스 DESC 마이그레이션 (Room version 2, Migration 객체와 함께)
2. `data ↔ edit` 패키지 순환 정리 (`CaptureConditions`·`SubjectBox`의 `data/` 이동 — 기존 리드 판정 "나중에")
3. 저장된 `capture_edit_stack` 파라미터로 편집기 복원(현재 쓰기 전용)
4. `+ 필터 저장` 흐름 (오너 보류)
5. 컷 항목 부활 여부 재검토 (시나리오 A·B, 피드백 루프)
